// AUTO-GENERATED by `faqir bindings vue` from registry/primitives/image/image.manifest.json.
// Do not edit — regenerate with `faqir bindings vue`.

import { defineFaqirPrimitive } from "../runtime";

/** Allowed `variant` values (manifest variant group "variant", attr `data-variant`). */
export type LImageVariant = "responsive" | "thumbnail" | "cover" | "contain";

/** Allowed `size` values (manifest variant group "size", attr `data-size`). */
export type LImageSize = "xs" | "sm" | "md" | "lg" | "full";

export interface LImageProps {
  /** `data-variant`; omitted when unset (manifest default: "responsive"). */
  variant?: LImageVariant;
  /** `data-size`; omitted when unset (manifest default: "md"). */
  size?: LImageSize;
}

/** `image` — Responsive image with variants for different display modes and print optimization */
export const LImage = defineFaqirPrimitive<LImageProps>({
  name: "image",
  tag: "figure",
  variants: [
    { prop: "variant", attr: "data-variant", values: ["responsive", "thumbnail", "cover", "contain"] },
    { prop: "size", attr: "data-size", values: ["xs", "sm", "md", "lg", "full"] },
  ],
  states: [],
  slots: [
    { name: "img", tag: "img", required: true, isVoid: true },
    { name: "caption", tag: "figcaption", required: false, isVoid: false },
  ],
  defaultSlot: false,
});

export default LImage;
