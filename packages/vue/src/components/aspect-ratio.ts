// AUTO-GENERATED by `faqir bindings vue` from registry/primitives/aspect-ratio/aspect-ratio.manifest.json.
// Do not edit — regenerate with `faqir bindings vue`.

import { defineFaqirPrimitive } from "../runtime";

/** Allowed `variant` values (manifest variant group "ratio", attr `data-variant`). */
export type LAspectRatioVariant = "square" | "16-9" | "4-3" | "3-2" | "21-9" | "portrait";

export interface LAspectRatioProps {
  /** `data-variant`; omitted when unset (manifest default: "square"). */
  variant?: LAspectRatioVariant;
}

/** `aspect-ratio` — CSS wrapper that locks a child (image, video, iframe) to a fixed width-to-height ratio and crops the overflow */
export const LAspectRatio = defineFaqirPrimitive<LAspectRatioProps>({
  name: "aspect-ratio",
  tag: "div",
  variants: [
    { prop: "variant", attr: "data-variant", values: ["square", "16-9", "4-3", "3-2", "21-9", "portrait"] },
  ],
  states: [],
  slots: [],
  defaultSlot: true,
});

export default LAspectRatio;
