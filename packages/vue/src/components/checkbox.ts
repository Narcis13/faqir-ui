// AUTO-GENERATED by `faqir bindings vue` from registry/primitives/checkbox/checkbox.manifest.json.
// Do not edit — regenerate with `faqir bindings vue`.

import { defineFaqirPrimitive } from "../runtime";

/** Allowed `size` values (manifest variant group "size", attr `data-size`). */
export type LCheckboxSize = "sm" | "md" | "lg";

export interface LCheckboxProps {
  /** `data-size`; omitted when unset (manifest default: "md"). */
  size?: LCheckboxSize;
  /** Sets the `checked` attribute. */
  checked?: boolean;
  /** Sets `data-state="indeterminate"`. */
  indeterminate?: boolean;
  /** Sets the `disabled` attribute. */
  disabled?: boolean;
}

/** `checkbox` — Custom styled checkbox with checked, indeterminate, disabled states and size variants */
export const LCheckbox = defineFaqirPrimitive<LCheckboxProps>({
  name: "checkbox",
  tag: "input",
  variants: [
    { prop: "size", attr: "data-size", values: ["sm", "md", "lg"] },
  ],
  states: [
    { prop: "checked", attr: "checked", value: null, kind: "presence" },
    { prop: "indeterminate", attr: "data-state", value: "indeterminate", kind: "value" },
    { prop: "disabled", attr: "disabled", value: null, kind: "presence" },
  ],
  slots: [],
  defaultSlot: false,
});

export default LCheckbox;
