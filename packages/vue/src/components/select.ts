// AUTO-GENERATED by `faqir bindings vue` from registry/primitives/select/select.manifest.json.
// Do not edit — regenerate with `faqir bindings vue`.

import { defineFaqirPrimitive } from "../runtime";

/** Allowed `size` values (manifest variant group "size", attr `data-size`). */
export type LSelectSize = "sm" | "md" | "lg";

export interface LSelectProps {
  /** `data-size`; omitted when unset (manifest default: "md"). */
  size?: LSelectSize;
  /** Sets `data-state="error"`. */
  error?: boolean;
  /** Sets the `disabled` attribute. */
  disabled?: boolean;
}

/** `select` — Native select dropdown with custom arrow, error and disabled states, and size variants */
export const LSelect = defineFaqirPrimitive<LSelectProps>({
  name: "select",
  tag: "select",
  variants: [
    { prop: "size", attr: "data-size", values: ["sm", "md", "lg"] },
  ],
  states: [
    { prop: "error", attr: "data-state", value: "error", kind: "value" },
    { prop: "disabled", attr: "disabled", value: null, kind: "presence" },
  ],
  slots: [],
  defaultSlot: true,
});

export default LSelect;
