// AUTO-GENERATED by `faqir bindings vue` from registry/primitives/signature/signature.manifest.json.
// Do not edit — regenerate with `faqir bindings vue`.

import { defineFaqirPrimitive } from "../runtime";

/** Allowed `size` values (manifest variant group "size", attr `data-size`). */
export type LSignatureSize = "sm" | "md" | "lg";

/** Allowed `align` values (manifest variant group "align", attr `data-align`). */
export type LSignatureAlign = "left" | "center" | "right";

export interface LSignatureProps {
  /** `data-size`; omitted when unset (manifest default: "md"). */
  size?: LSignatureSize;
  /** `data-align`; omitted when unset (manifest default: "left"). */
  align?: LSignatureAlign;
}

/** `signature` — Signature line with label for printed document signing */
export const LSignature = defineFaqirPrimitive<LSignatureProps>({
  name: "signature",
  tag: "div",
  variants: [
    { prop: "size", attr: "data-size", values: ["sm", "md", "lg"] },
    { prop: "align", attr: "data-align", values: ["left", "center", "right"] },
  ],
  states: [],
  slots: [
    { name: "line", tag: "div", required: true, isVoid: false },
    { name: "label", tag: "span", required: true, isVoid: false },
  ],
  defaultSlot: false,
});

export default LSignature;
