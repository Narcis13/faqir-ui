// AUTO-GENERATED by `faqir bindings vue` from registry/primitives/link/link.manifest.json.
// Do not edit — regenerate with `faqir bindings vue`.

import { defineFaqirPrimitive } from "../runtime";

/** Allowed `variant` values (manifest variant group "style", attr `data-variant`). */
export type LLinkVariant = "default" | "external" | "muted";

export interface LLinkProps {
  /** `data-variant`; omitted when unset (manifest default: "default"). */
  variant?: LLinkVariant;
}

/** `link` — Styled anchor with external/muted variants; external links get a CSS-only indicator */
export const LLink = defineFaqirPrimitive<LLinkProps>({
  name: "link",
  tag: "a",
  variants: [
    { prop: "variant", attr: "data-variant", values: ["default", "external", "muted"] },
  ],
  states: [],
  slots: [],
  defaultSlot: true,
});

export default LLink;
