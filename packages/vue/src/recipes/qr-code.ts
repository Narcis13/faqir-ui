// AUTO-GENERATED by `faqir bindings vue` from registry/recipes/qr-code/qr-code.manifest.json
// and the recipe controller. Do not edit — regenerate with `faqir bindings vue`.

import { defineFaqirRecipe } from "../recipe-runtime";
import type { RecipeNode } from "../recipe-runtime";
import { createQRCode } from "../controllers/qr-code";

/** Allowed `ecl` values (manifest variant group "ecl", attr `data-ecl` on root). */
export type LQrCodeEcl = "L" | "M" | "Q" | "H";

/** Allowed `size` values (manifest variant group "size", attr `data-size` on root). */
export type LQrCodeSize = "sm" | "md" | "lg";

export interface LQrCodeProps {
  /** Root/ARIA id base; auto-generated per instance when unset. */
  id?: string;
  /** Template text/attr substitution (default: ""). */
  value?: string;
  /** `data-ecl` on root; omitted when unset (manifest default: "M"). */
  ecl?: LQrCodeEcl;
  /** `data-size` on root; omitted when unset (manifest default: "md"). */
  size?: LQrCodeSize;
}

/** `qr-code` — SVG QR code generator from text input with configurable size and error correction Exposes: `render()`, `update()`. */
export const LQrCode = defineFaqirRecipe<LQrCodeProps>({
  name: "qr-code",
  create: createQRCode,
  methods: ["render", "update"],
  events: [],
  stringProps: [{ prop: "value", default: "" }],
  boolProps: [],
  variantProps: [
    { prop: "ecl", values: ["L", "M", "Q", "H"] },
    { prop: "size", values: ["sm", "md", "lg"] },
  ],
  tree: {"tag":"div","attrs":[["data-ui",["qr-code"]],["data-value",[{"p":"value"}]],["role",["img"]],["aria-label",["QR code: "]]],"children":[],"dyn":[["size","data-size"],["ecl","data-ecl"]]} as unknown as RecipeNode,
});

export default LQrCode;
