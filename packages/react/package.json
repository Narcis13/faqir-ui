{
  "name": "@faqir-ui/react",
  "version": "0.1.0",
  "description": "React bindings for Faqir UI — RSC-safe components generated from registry manifests by `faqir bindings react`; ships no CSS",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "//exports": "Points at dist/, never at src/. The package used to resolve `main`, `module`, `types` and every `exports` condition to `./src/index.ts`, so the tarball held 108 TypeScript files and zero JS or declarations. That is not installable: Node 18 fails with ERR_UNKNOWN_FILE_EXTENSION, Node 24 with ERR_UNSUPPORTED_NODE_MODULES_TYPE_STRIPPING, `tsc --moduleResolution node16` reports errors `skipLibCheck` cannot suppress (it skips .d.ts, and those were .ts), and a stock `next build` dies in `Module parse failed`. Only Vite worked, and Next only with `transpilePackages` — neither README said so. `tests/bindings/packaged-consumer.test.ts` installs the packed tarball and holds this shape.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\" && tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run build",
    "test": "bun test",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "peerDependencies": {
    "react": "^18.2.0 || ^19.0.0",
    "react-dom": "^18.2.0 || ^19.0.0"
  },
  "peerDependenciesMeta": {
    "react-dom": {
      "optional": true
    }
  },
  "devDependencies": {
    "@testing-library/dom": "^10.4.0",
    "@testing-library/react": "^16.1.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "keywords": [
    "faqir",
    "react",
    "rsc",
    "components",
    "bindings",
    "manifest-driven"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Narcis13/faqir-ui.git",
    "directory": "packages/react"
  },
  "homepage": "https://github.com/Narcis13/faqir-ui/tree/main/packages/react#readme",
  "bugs": {
    "url": "https://github.com/Narcis13/faqir-ui/issues"
  },
  "engines": {
    "node": ">=18"
  },
  "publishConfig": {
    "access": "public"
  }
}
