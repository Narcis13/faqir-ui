// AUTO-GENERATED by `faqir bindings react` from registry/primitives/switch/switch.manifest.json.
// Do not edit — regenerate with `faqir bindings react`.

import { createFaqirPrimitive } from "../runtime";
import type { ComponentPropsWithoutRef, ReactNode } from "react";

/** Allowed `size` values (manifest variant group "size", attr `data-size`). */
export type LSwitchSize = "sm" | "md" | "lg";

export interface LSwitchProps extends Omit<ComponentPropsWithoutRef<"button">, "size" | "on" | "disabled" | "label"> {
  /** `data-size`; omitted when unset (manifest default: "md"). */
  size?: LSwitchSize;
  /** Sets `data-state="on"`. */
  on?: boolean;
  /** Sets the `disabled` attribute. */
  disabled?: boolean;
  /** Projected into `<span data-part="label">`. */
  label?: ReactNode;
}

/** `switch` — Toggle switch with on/off states, disabled state, and size variants */
export const LSwitch = createFaqirPrimitive<LSwitchProps>({
  name: "switch",
  tag: "button",
  variants: [
    { prop: "size", attr: "data-size", values: ["sm", "md", "lg"] },
  ],
  states: [
    { prop: "on", attr: "data-state", value: "on", kind: "value" },
    { prop: "disabled", attr: "disabled", value: null, kind: "presence" },
  ],
  slots: [
    { name: "label", tag: "span", required: false, isVoid: false },
  ],
  defaultSlot: true,
}, "LSwitch");

export default LSwitch;
