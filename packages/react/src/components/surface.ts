// AUTO-GENERATED by `faqir bindings react` from registry/primitives/surface/surface.manifest.json.
// Do not edit — regenerate with `faqir bindings react`.

import { createFaqirPrimitive } from "../runtime";
import type { ComponentPropsWithoutRef } from "react";

/** Allowed `variant` values (manifest variant group "elevation", attr `data-variant`). */
export type LSurfaceVariant = "flat" | "raised" | "overlay";

/** Allowed `size` values (manifest variant group "padding", attr `data-size`). */
export type LSurfaceSize = "0" | "sm" | "md" | "lg";

export interface LSurfaceProps extends Omit<ComponentPropsWithoutRef<"div">, "variant" | "size"> {
  /** `data-variant`; omitted when unset (manifest default: "default"). */
  variant?: LSurfaceVariant;
  /** `data-size`; omitted when unset (manifest default: "0"). */
  size?: LSurfaceSize;
}

/** `surface` — Generic container with visual elevation and configurable padding */
export const LSurface = createFaqirPrimitive<LSurfaceProps>({
  name: "surface",
  tag: "div",
  variants: [
    { prop: "variant", attr: "data-variant", values: ["flat", "raised", "overlay"] },
    { prop: "size", attr: "data-size", values: ["0", "sm", "md", "lg"] },
  ],
  states: [],
  slots: [],
  defaultSlot: true,
}, "LSurface");

export default LSurface;
