// AUTO-GENERATED by `faqir bindings react` from registry/primitives/radio/radio.manifest.json.
// Do not edit — regenerate with `faqir bindings react`.

import { createFaqirPrimitive } from "../runtime";
import type { ComponentPropsWithoutRef } from "react";

/** Allowed `size` values (manifest variant group "size", attr `data-size`). */
export type LRadioSize = "sm" | "md" | "lg";

export interface LRadioProps extends Omit<ComponentPropsWithoutRef<"input">, "size" | "checked" | "disabled"> {
  /** `data-size`; omitted when unset (manifest default: "md"). */
  size?: LRadioSize;
  /** Sets the `checked` attribute. */
  checked?: boolean;
  /** Sets the `disabled` attribute. */
  disabled?: boolean;
}

/** `radio` — Custom styled radio button for single selection within a group, with size variants */
export const LRadio = createFaqirPrimitive<LRadioProps>({
  name: "radio",
  tag: "input",
  variants: [
    { prop: "size", attr: "data-size", values: ["sm", "md", "lg"] },
  ],
  states: [
    { prop: "checked", attr: "checked", value: null, kind: "presence" },
    { prop: "disabled", attr: "disabled", value: null, kind: "presence" },
  ],
  slots: [],
  defaultSlot: false,
}, "LRadio");

export default LRadio;
