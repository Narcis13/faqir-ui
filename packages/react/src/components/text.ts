// AUTO-GENERATED by `faqir bindings react` from registry/primitives/text/text.manifest.json.
// Do not edit — regenerate with `faqir bindings react`.

import { createFaqirPrimitive } from "../runtime";
import type { ComponentPropsWithoutRef } from "react";

/** Allowed `variant` values (manifest variant group "variant", attr `data-variant`). */
export type LTextVariant = "default" | "muted" | "subtle" | "primary" | "mono";

/** Allowed `size` values (manifest variant group "size", attr `data-size`). */
export type LTextSize = "xs" | "sm" | "base" | "lg" | "xl" | "1" | "2" | "3" | "4" | "5" | "6";

/** Allowed `weight` values (manifest variant group "weight", attr `data-weight`). */
export type LTextWeight = "normal" | "medium" | "semibold" | "bold";

/** Allowed `align` values (manifest variant group "align", attr `data-align`). */
export type LTextAlign = "left" | "center" | "right";

/** Allowed `leading` values (manifest variant group "leading", attr `data-leading`). */
export type LTextLeading = "tight" | "snug" | "normal" | "relaxed";

export interface LTextProps extends Omit<ComponentPropsWithoutRef<"p">, "variant" | "size" | "weight" | "align" | "leading"> {
  /** `data-variant`; omitted when unset (manifest default: "default"). */
  variant?: LTextVariant;
  /** `data-size`; omitted when unset (manifest default: "base"). */
  size?: LTextSize;
  /** `data-weight`; omitted when unset (manifest default: "normal"). */
  weight?: LTextWeight;
  /** `data-align`; omitted when unset (manifest default: "left"). */
  align?: LTextAlign;
  /** `data-leading`; omitted when unset (manifest default: "normal"). */
  leading?: LTextLeading;
}

/** `text` — Text and heading primitives with size, color, weight, alignment, and truncation via data attributes */
export const LText = createFaqirPrimitive<LTextProps>({
  name: "text",
  tag: "p",
  variants: [
    { prop: "variant", attr: "data-variant", values: ["default", "muted", "subtle", "primary", "mono"] },
    { prop: "size", attr: "data-size", values: ["xs", "sm", "base", "lg", "xl", "1", "2", "3", "4", "5", "6"] },
    { prop: "weight", attr: "data-weight", values: ["normal", "medium", "semibold", "bold"] },
    { prop: "align", attr: "data-align", values: ["left", "center", "right"] },
    { prop: "leading", attr: "data-leading", values: ["tight", "snug", "normal", "relaxed"] },
  ],
  states: [],
  slots: [],
  defaultSlot: true,
}, "LText");

export default LText;
