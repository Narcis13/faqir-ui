// AUTO-GENERATED by `faqir bindings react` from registry/primitives/container/container.manifest.json.
// Do not edit — regenerate with `faqir bindings react`.

import { createFaqirPrimitive } from "../runtime";
import type { ComponentPropsWithoutRef } from "react";

/** Allowed `measure` values (manifest variant group "measure", attr `data-measure`). */
export type LContainerMeasure = "narrow" | "content" | "wide" | "prose" | "full";

export interface LContainerProps extends Omit<ComponentPropsWithoutRef<"div">, "measure" | "measureSm" | "measureMd" | "measureLg" | "measureXl"> {
  /** `data-measure`; omitted when unset (manifest default: "content"). */
  measure?: LContainerMeasure;
  /** `data-measure-sm` — the `measure` value from the `sm` breakpoint up; omitted when unset. */
  measureSm?: LContainerMeasure;
  /** `data-measure-md` — the `measure` value from the `md` breakpoint up; omitted when unset. */
  measureMd?: LContainerMeasure;
  /** `data-measure-lg` — the `measure` value from the `lg` breakpoint up; omitted when unset. */
  measureLg?: LContainerMeasure;
  /** `data-measure-xl` — the `measure` value from the `xl` breakpoint up; omitted when unset. */
  measureXl?: LContainerMeasure;
}

/** `container` — A centred measure column: caps the line length at one of the shared measure tokens and centres what is left, with optional inline gutter padding. Not to be confused with the CSS `container-type` feature — a container establishes no containment context and queries nothing; the component that measures its own inline size is `switcher` */
export const LContainer = createFaqirPrimitive<LContainerProps>({
  name: "container",
  tag: "div",
  variants: [
    { prop: "measure", attr: "data-measure", values: ["narrow", "content", "wide", "prose", "full"] },
    { prop: "measureSm", attr: "data-measure-sm", values: ["narrow", "content", "wide", "prose", "full"] },
    { prop: "measureMd", attr: "data-measure-md", values: ["narrow", "content", "wide", "prose", "full"] },
    { prop: "measureLg", attr: "data-measure-lg", values: ["narrow", "content", "wide", "prose", "full"] },
    { prop: "measureXl", attr: "data-measure-xl", values: ["narrow", "content", "wide", "prose", "full"] },
  ],
  states: [],
  slots: [],
  defaultSlot: true,
}, "LContainer");

export default LContainer;
