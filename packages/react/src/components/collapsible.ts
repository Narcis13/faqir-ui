// AUTO-GENERATED by `faqir bindings react` from registry/primitives/collapsible/collapsible.manifest.json.
// Do not edit — regenerate with `faqir bindings react`.

import { createFaqirPrimitive } from "../runtime";
import type { ComponentPropsWithoutRef, ReactNode } from "react";

/** Allowed `variant` values (manifest variant group "visual", attr `data-variant`). */
export type LCollapsibleVariant = "default" | "bordered";

export interface LCollapsibleProps extends Omit<ComponentPropsWithoutRef<"details">, "variant" | "open" | "trigger" | "content"> {
  /** `data-variant`; omitted when unset (manifest default: "default"). */
  variant?: LCollapsibleVariant;
  /** Sets the `open` attribute. */
  open?: boolean;
  /** Projected into `<summary data-part="trigger">` (required part — always rendered). */
  trigger?: ReactNode;
  /** Projected into `<div data-part="content">` (required part — always rendered). */
  content?: ReactNode;
}

/** `collapsible` — Disclosure widget built on native <details>/<summary> — opens and closes with zero JavaScript, animated via ::details-content where supported */
export const LCollapsible = createFaqirPrimitive<LCollapsibleProps>({
  name: "collapsible",
  tag: "details",
  variants: [
    { prop: "variant", attr: "data-variant", values: ["default", "bordered"] },
  ],
  states: [
    { prop: "open", attr: "open", value: null, kind: "presence" },
  ],
  slots: [
    { name: "trigger", tag: "summary", required: true, isVoid: false },
    { name: "content", tag: "div", required: true, isVoid: false },
  ],
  defaultSlot: false,
}, "LCollapsible");

export default LCollapsible;
