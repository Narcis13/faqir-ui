// AUTO-GENERATED by `faqir bindings react` from registry/primitives/nav/nav.manifest.json.
// Do not edit — regenerate with `faqir bindings react`.

import { createFaqirPrimitive } from "../runtime";
import type { ComponentPropsWithoutRef, ReactNode } from "react";

/** Allowed `variant` values (manifest variant group "variant", attr `data-variant`). */
export type LNavVariant = "horizontal" | "vertical" | "underline" | "pill";

export interface LNavProps extends Omit<ComponentPropsWithoutRef<"nav">, "variant" | "link" | "separator"> {
  /** `data-variant`; omitted when unset (manifest default: "horizontal"). */
  variant?: LNavVariant;
  /** Projected into `<a data-part="link">` (required part — always rendered). */
  link?: ReactNode;
  /** Projected into `<div data-part="separator">`. */
  separator?: ReactNode;
}

/** `nav` — Navigation bar with link slots, active state, and horizontal/vertical/underline/pill variants */
export const LNav = createFaqirPrimitive<LNavProps>({
  name: "nav",
  tag: "nav",
  variants: [
    { prop: "variant", attr: "data-variant", values: ["horizontal", "vertical", "underline", "pill"] },
  ],
  states: [],
  slots: [
    { name: "link", tag: "a", required: true, isVoid: false },
    { name: "separator", tag: "div", required: false, isVoid: false },
  ],
  defaultSlot: false,
}, "LNav");

export default LNav;
