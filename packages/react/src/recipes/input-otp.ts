// AUTO-GENERATED by `faqir bindings react` from registry/recipes/input-otp/input-otp.manifest.json
// and the recipe controller. Do not edit — regenerate with `faqir bindings react`.

"use client";

import { createFaqirRecipe } from "../recipe-runtime";
import type { FaqirEventHandler, RecipeNode } from "../recipe-runtime";
import type { ComponentPropsWithoutRef, ReactNode } from "react";
import { createInputOTP } from "../controllers/input-otp";

/** Allowed `mode` values (manifest variant group "mode", attr `data-mode` on root). */
export type LInputOtpMode = "numeric" | "alphanumeric";

/** Allowed `size` values (manifest variant group "size", attr `data-size` on root). */
export type LInputOtpSize = "sm" | "md" | "lg";

export interface LInputOtpProps extends Omit<ComponentPropsWithoutRef<"div">, "id" | "mode" | "size" | "input" | "segments" | "onChange" | "onComplete"> {
  /** Root/ARIA id base; auto-generated per instance when unset. */
  id?: string;
  /** `data-mode` on root; omitted when unset (manifest default: "numeric"). */
  mode?: LInputOtpMode;
  /** `data-size` on root; omitted when unset (manifest default: "md"). */
  size?: LInputOtpSize;
  /** Content projected into the `[data-part="input"]` element. */
  input?: ReactNode;
  /** Content projected into the `[data-part="segments"]` element. */
  segments?: ReactNode;
  /** `faqir:change` → `(detail, event)`. */
  onChange?: FaqirEventHandler;
  /** `faqir:complete` → `(detail, event)`. */
  onComplete?: FaqirEventHandler;
}

/** `input-otp` — Segmented one-time-code input — N visual segments over a single hidden real input, with paste distribution, auto-advance, optional l-mask enforcement, and a fire-once complete event Emits: `onChange`, `onComplete` (payload: `(detail, event)`). Exposes via ref: `getValue()`, `setValue()`, `clear()`, `focus()`. */
export const LInputOtp = createFaqirRecipe<LInputOtpProps>({
  name: "input-otp",
  create: createInputOTP,
  methods: ["getValue", "setValue", "clear", "focus"],
  events: ["change", "complete"],
  slots: ["input", "segments"],
  stringProps: [],
  boolProps: [],
  variantProps: [
    { prop: "mode", values: ["numeric", "alphanumeric"] },
    { prop: "size", values: ["sm", "md", "lg"] },
  ],
  tree: {"tag":"div","attrs":[["data-ui",["input-otp"]],["data-length",["6"]],["data-mode",["numeric"]]],"children":[{"tag":"input","attrs":[["data-part",["input"]],["type",["text"]],["inputmode",["numeric"]],["autocomplete",["one-time-code"]],["pattern",["[0-9]*"]],["maxlength",["6"]],["autocapitalize",["off"]],["autocorrect",["off"]],["spellcheck",["false"]],["aria-label",["One-time code"]]],"children":[],"slot":"input"},{"tag":"div","attrs":[["data-part",["segments"]],["aria-hidden",["true"]]],"children":[{"tag":"div","attrs":[["data-part",["segment"]]],"children":[]},{"tag":"div","attrs":[["data-part",["segment"]]],"children":[]},{"tag":"div","attrs":[["data-part",["segment"]]],"children":[]},{"tag":"div","attrs":[["data-part",["segment"]]],"children":[]},{"tag":"div","attrs":[["data-part",["segment"]]],"children":[]},{"tag":"div","attrs":[["data-part",["segment"]]],"children":[]}],"slot":"segments"}],"dyn":[["size","data-size"],["mode","data-mode"]]} as unknown as RecipeNode,
}, "LInputOtp");

export default LInputOtp;
