{
  "$schema": "./state.schema.md",
  "plan": "FAQIR-PLAN.md",
  "current": null,
  "last_completed": "1.0R-09",
  "history": [
    {
      "id": "1.0R-09",
      "completed": "2026-08-19",
      "commit": "69e792a",
      "summary": "The engine got a page and the two layout URLs stopped being one keystroke apart. `engine/index.html` is authored prose and live examples around five GENERATED tables \u2014 17 directives, 18 modifiers, 11 magics plus the named internal, the PRIORITY ladder, the KEY_MAP aliases, the MOTION_PRESETS, the `$<name>` controller API off the `ctrl` literal and a row per plugin \u2014 read through the same parsers 1.0R-03 wrote for references/directives.md, so the site and the skill are two renderings of one vocabulary rather than two lists. The page loads scripts/faqir-core.js, so its examples ARE the engine running. NEW tests/generator/engine-page.test.ts mounts every authored example under faqir-core.dev.js (one engine per file, per the dev-build convention) and asserts it binds, reacts and draws ZERO diagnostic: the counter counts, `.trim` puts the trimmed value in the scope, the keyed list reconciles a removal by key and then reveals its `l-if` empty state, `l-show` flips and stamps `data-motion`, and `l-source` loads the site's own api/messages payload and renders a row per message. THAT TEST FOUND A SHIPPED BUG ON ITS FIRST RUN: `parseGuideExamples` ended an example at the FIRST `</template>` \u2014 fine for spacing/density, wrong for the first guide whose examples are templates, since `l-for` and `l-if` ARE `<template>` directives, so the keyed-list example shipped truncated at its own l-for's closing tag. It audited clean anyway (the audit tolerates an unclosed element) and rendered as something (a browser closes what the author did not). The extractor now counts depth and renderGuideExamples mounts from the same scan. A second latent bug fell out of the same page: replaceGuideMarker passed its block to String.replace as a STRING, which reads `$&` and `$<name>` as substitution patterns \u2014 and the l-source controller is literally called `$<name>`, so the engine table ate it; it is a replacer function now. THE ROUTE HALF WAS A JUDGEMENT CALL, ARGUED RATHER THAN DEFAULTED: the lab moved /layouts/ \u2192 /responsive/ and the guide keeps /layout/, and the retired URL is a SIGNPOST, NOT A REDIRECT \u2014 a 301 fixes the bookmark and keeps the defect, because anyone landing on /layouts/ may have been aiming at /layout/, and quietly serving them the lab is the confusion the task removes. The signpost names both destinations, carries noindex, and is filtered out of the sitemap. Asserted as a PROPERTY over the whole published route set rather than for the one pair: every pair of top-level routes is \u2265 2 keystrokes apart by Levenshtein (the ruler itself checked), with the retired path the single exemption \u2014 because it is the exemption. Also: no page, nav entry or sitemap row points at a retired path; site/content/layouts.html \u2192 responsive.html; home and docs/docs-site.md follow. DELIBERATELY NOT DONE: the engine is in every page's sidebar and on the home page but NOT in the top nav \u2014 that bar is `overflow: hidden` with nine links at 1280, a tenth would clip an existing one silently, and the layout ratchet cannot see a clip. Verified: 3,516 bun tests, typecheck, check:docs (331 files), check:skill, check:registry-index, check:audit-browser, check:schema-refs, size, audit:registry, check:bindings; a11y green on engine/responsive/layouts in both schemes (six timeouts in the full 20-minute run were load flakes and pass on re-run); layout budget re-recorded at 194 pages with the three new/moved pages at 0/0/0 at BOTH viewports and every ratcheted total unchanged. Five mutations checked: a dropped directive row, a dropped magic row, a trimmed plugin `provides`, a route one keystroke from another, a sitemap advertising a retired path, and a signpost linking one destination twice \u2014 the last two only after tightening the assertions, which is how the first pass of each was found to pass a broken page (the nav shell and the priority paragraph both name what the deleted TABLE ROW named)."
    },
    {
      "id": "1.0R-08",
      "completed": "2026-08-19",
      "commit": "4785b4f",
      "summary": "The five scaffolds get a gallery generated from SCAFFOLDS, and admin-dashboard/internal-tool get real builders composed out of the patterns' own @ui:scaffold blocks (303 lines of hand-written, audit-failing markup deleted). Recorded here by 1.0R-09: the work shipped in 4785b4f with every acceptance box ticked, but the index cell and this cursor were never flipped."
    },
    {
      "id": "1.0R-07",
      "completed": "2026-08-18",
      "commit": null,
      "summary": "Spent the budget 1.0R-06 recorded: 375\u00d7812 bleeds 52 \u2192 29, with all five target pages at ZERO and 1280\u00d7900 re-recorded identically on all five counts (the 968-case desktop matrix for the touched components is byte-identical too, so nothing was traded). Every fix is intrinsic, per the doctrine the framework teaches \u2014 flex-wrap, max-inline-size, min-inline-size, overflow-wrap \u2014 and `breakpoint-canon` sees no new width prelude because none was added. `pagination`: the numbered nav neither wrapped nor had a ceiling, and an `inline-flex` root is sized by its content, so the row set the width. `key-value`: a 24-character IBAN IS the `1fr` track's min-content, so one unbreakable value sized the whole grid and every later row bled with it \u2014 `overflow-wrap: anywhere` rather than `break-word`, because only `anywhere` lowers the min-content contribution the track is floored by. `empty-state`: the centred cluster overflowed past BOTH edges, the inline-start half unreachable at any scroll position. BOTH JUDGEMENT CALLS CAME OUT 'same mechanism, fix them': `input-otp` (six `lg` segments and their gaps are 372px, wider than a 375px window before the page gutter; the transparent input is `inset: 0` so it follows whatever box the segments settle into) and `menubar`'s trigger bar (the same unwrapped row, 198px past its own example page). THE REAL CAUSE OF empty-state's BLEED WAS CONTRACT DRIFT, NOT A MISSING DECLARATION: 0.9-04 aligned the pattern's markup and slots onto the declared `actions` cluster but left the sheet styling `action`/`secondary-action` \u2014 parts nothing renders \u2014 so the slot that exists had no rule of its own and inherited a non-wrapping row from the PRIMITIVE of the same name (0.9-13's unresolved collision). Both sheets now style `actions` and the pattern's manifest template stops emitting the two phantom parts, so `faqir add` cannot reproduce the defect. NEW: tests/visual/narrow-fit.pw.ts \u2014 the wall beside the ratchet, per component, no budget, affordable only because the set is at zero. Each reference is mounted ALONE (buildPageHtml, shipped bytes) at 320 and 375, and again in a container 60% of its own max-content width. THE SECOND QUESTION IS NOT DECORATION: `menubar` fits a 375px window in isolation at 229px wide and still bled 198px on a page offering it 343, so a window-only check would have pinned nothing for it \u2014 and measuring max-content on `<main>` does not work either, because pagination/input-otp root at `inline-flex` and their demos share one inline formatting context, making main's max-content the whole row laid end to end. Each root is therefore sized to `max-content` on its own. All five fixes are pinned by a case that reverts exactly that declaration and asserts the named box reappears, then re-runs the page unbroken and expects silence; all five bite. DEFERRED, WITH A REPRODUCTION: `menubar`'s open submenu is a different mechanism \u2014 an anchored dropdown with no viewport clamp. Making the bar wrap took the example page to 0 by moving the anchor, not by clamping the panel; a five-trigger bar at 375 wraps so the open group lands mid-row and the submenu still paints 44px past the edge. Filed as 1.0R-12 as the family question it is (context-menu, dropdown-menu, select-custom, popover, tooltip anchor the same way, and the ratchet cannot see any of them because every one is closed on its example page). ALSO REPAIRED: the index cells for 1.0R-05 and 1.0R-06, which shipped with every acceptance box ticked but were never flipped to \u2705. Verified: bun test 3482/0, typecheck clean, full playwright 4291/0, a11y 2985/0, audit:registry zero findings across six rule sets, all six generated-artifact checks up to date, layout gate + wall green on three consecutive runs. The one baseline that moved is the proof: responsive-pattern-empty-state-390 was 399px WIDE at a 390px viewport and is now 390. NOTE: packages/core/cdn.json is regenerated here \u2014 12 theme hashes because this task changes registry CSS, plus 3 JS hashes (faqir-core.js, its map, faqir-intersect.js) that were already stale against a fresh build."
    },
    {
      "id": "1.0R-06",
      "completed": "2026-08-18",
      "commit": null,
      "summary": "The layout ratchet gained a second ruler, and the second ruler found that the first one had been reporting a phone-width site nobody measured. `layout-budget.json` is now one section per viewport (`{viewports: {\"1280x900\": {...}, \"375x812\": {...}}}`, widest first); `summarize` returns one section, `compareBudget` still compares WITHIN a viewport, and the new `compareBudgets` runs the ratchet per section so a fall at 1280 cannot pay for a rise at 375 (pinned in both directions, plus the trade-off case). Two bookkeeping states the shape creates are decided rather than left implicit: a viewport measured but not yet budgeted is `unbudgeted` and PASSES (a rise against a section that does not exist would have deadlocked update mode against its own refusal to record one), while a viewport budgeted and no longer measured FAILS \u2014 a sweep that quietly drops a viewport reports a perfect site. Same Playwright job, same file, ~7s more: `beforeAll` sweeps `VIEWPORTS` in one context each. MEASURING 375 HONESTLY COST TWO COLLECTOR RULES AND A FLAKE FIX, all three forced by real evidence and none of them touching a desktop number (the 1280 section re-recorded byte-identical). (1) The first narrow sweep read 12,516 bleeds \u2014 110 shell pages \u00d7 124 boxes of the docs shell's CLOSED off-canvas sidebar, parked at x:-288 behind an `overflow-x: hidden` shell, painting nothing. The bleed subject is now the box intersected with the padding box of every clipping ancestor: an empty slice is dropped, a slice that still crosses the edge is counted AT THE WIDTH IT PAINTS (a 400px child of a clipped card reports 45, not 325). (2) That left `recipes/sidebar` at 58 \u2014 its dismissed mobile drawer at translateX(-100%) with nothing clipping it. `inert` (not `aria-hidden`, which would also swallow every decorative icon) is the signal: a subtree that is not focusable, announced or clickable is not content the reader was offered. Both exemptions are pinned by a synthetic page where three panels sit at the SAME coordinates for three different reasons and only the stranded one \u2014 off-canvas with neither excuse, i.e. in the tab order and unreachable \u2014 is reported. (3) The sweep was flaky at 375 (1 run in 3): the sidebar controller reconciles to its closed drawer on init, starting a `transform` transition, and the collector was reading geometry mid-slide. Fixed by measuring the settled page \u2014 the sweep context now sets `reducedMotion: \"reduce\"` (the site's own recipes carry `transition: none` blocks for it, and it is what playwright.config.ts already throws for screenshots) and `settleMotion()` finishes every finite animation before collection, no sleeps in 372 page loads. Five consecutive green runs. The one hard wall stays desktop-only and says so ('no reference page has unreachable viewport bleed at desktop width'); 375 gets the ratchet, which is the point of the task \u2014 the numbers are recorded, not exempted, and 1.0R-07 spends them. Committed: 375\u00d7812 at 52 bleeds over 8 pages (dashboard-shell 14, settings-page 13, menubar 8, pagination 5, empty-state 4, input-otp 4, key-value 2, toast 2) and 3 overlaps; 1280\u00d7900 unchanged at 0/0/0/0/0 over 186 pages. That is a SUPERSET of the review's six \u2014 boxes, not one px number per page, and it reaches the inline start, where a right-edge measure cannot look. Verified: 8/8 playwright layout-lint (5 repeat runs), 65 bun cases in layout-lint+rhythm (13 new), typecheck. PRE-EXISTING, NOT CAUSED HERE: the CDN-pin case in docs-agents.test.ts fails on a clean tree too (stale local packages/core sri.json) \u2014 3481/3482 otherwise."
    },
    {
      "id": "1.0R-05",
      "completed": "2026-08-18",
      "commit": null,
      "summary": "Three promises the frontmatter made and the body did not keep, all three closed by derivation rather than by writing the missing prose. (1) The CLI list was hand-written and five commands behind the registry \u2014 doctor, variant, scaffold, dev, bindings were dispatched by the CLI and invisible to the skill. Fixed at the source: src/command-registry.ts now holds COMMAND_DEFINITIONS, one entry per command carrying handler + category + args + summary + skillNote, so the TYPE makes it impossible to register a command without the metadata both readers need; COMMANDS is derived from it for dispatch, src/index.ts's HELP_CATEGORIES (the second, drifting copy) is deleted and `faqir help` groups straight off the registry, and renderCliReference() emits `faqir <name> <args>  # <skillNote>` per category. (2) `faqir scaffold` was never named in the body at all, so an agent asked for an invoice hand-composed one instead of running the print-tested generator. The catalogue moved out of src/commands/scaffold.ts into a side-effect-free src/scaffolds/registry.ts (SCAFFOLDS, SCAFFOLD_NAMES, DOCUMENT_SCAFFOLD_NAMES); both the command's --help and the new Scaffolds section read it, and the section states the FAQIR_REPLACE convention so the placeholders are usable. (3) Not one of the twelve shipped themes was named, so \"make it dark\" had nothing to pass. The Themes section is a row per registry/themes/*.theme.json with the manifest's own mood / scheme / dark_mode / pairs_with \u2014 the agent-selectable axis those manifests exist for. Both sections go into the project skill too, which is what keeps the CLI reference's pointers resolvable in both. The frontmatter gained the theme claim it was missing, and FRONTMATTER_CAPABILITIES pairs each advertised capability with the section that delivers it \u2014 asserted in both directions, since the frontmatter is the routing surface and a promise with nothing behind it routes work to a skill that cannot answer it. Tests: 21 new cases across four describes \u2014 every COMMAND_NAMES entry documented and nothing invented, each command's own args/gloss verbatim, a synthetic command/scaffold/theme.json each picked up with no generator edit, every theme row cross-read against its manifest, and every dark-moods theme confirmed to have a real .css to pass. Verified: 63 pass in skill.test.ts (mutation-checked \u2014 dropping one theme row fails four cases), 3436/3437 full suite, typecheck, check:skill, check:docs, smoke. PRE-EXISTING, NOT CAUSED HERE: docs-agents.test.ts's CDN-pin case fails on a clean tree too (local packages/core sri.json is stale)."
    },
    {
      "id": "1.0-01",
      "completed": "2026-08-17",
      "commit": null,
      "summary": "The freeze, with a mechanism. SPEC-1.0.md is a NEW document rather than a section of FAQIR-SPEC.md \u2014 that file is 2,400 lines of design record, and a frozen contract cannot live inside a document that is free to keep changing; \u00a72 there now opens with a pointer saying which of the two is normative. The spec says only what an implementation must do: the five attributes with their owner/vocabulary/enforcing-rule per row, one shared value grammar (^[a-z0-9]+(?:-[a-z0-9]+)*$, never a list \u2014 which is what keeps [data-variant=\"x\"] an equality match), the three sanctioned token modifiers (data-density compact|comfortable, data-motion enter|enter-active|leave|leave-active, data-theme light|dark|auto) as attributes that are frozen WITHOUT being a sixth protocol attribute, the v0.8 data-<attr>-<tier> suffix derived from the canon, manifest schema 1.0, enforcement, the amendment process, and the 0.x changelog. The half that makes it survive: src/protocol.ts carries the same contract as DATA (ATTRIBUTE_SPECS, TOKEN_MODIFIERS, PROTOCOL_RULES, RESPONSIVE_RULES, AMENDMENT_RULES, SCHEMA_CHANGELOG), the spec places an HTML-comment marker above each normative table, and tests/spec/protocol-1.0.test.ts (39 cases) parses them back out and compares row for row in both directions \u2014 prose that agrees with the engine only on the day it was written is exactly what a freeze cannot survive. Executable documentation is taken literally: EVERY fenced block is verified by the checker that fits it \u2014 seven html examples audit to zero findings at every severity against the shipped manifests, the css block is asserted to obey the attribute-selector rule it demonstrates, the json block validates against the schema's own variant/prop definitions, the text grammar block is compared to the compiled pattern, and an unrecognised fence language FAILS rather than being quietly exempted. The registry is checked in the other direction too: every data-part, data-state, data-variant and data-size value the 86 components ship is asserted to obey the published grammar. Schema: schema_version 1.0 (not 1.0.0 \u2014 the patch digit says nothing under 'additive only'; the file had claimed 1.0.0 since its first commit, before there was a freeze to back it, and the changelog row records that rather than tidying it away), plus stability, protocol_version, amendment_policy pointing at the #amendments anchor the page actually emits, and its own changelog array mirroring src/protocol.ts. Site: one versioned prefix built from PROTOCOL_VERSION rather than written out \u2014 spec/1.0/index.html (generated from the module, rendering the same example bytes the suite audited), spec/1.0/spec.md (byte-identical to the repo file) and spec/1.0/manifest.schema.json (byte-identical to the $id alias, which stays as the always-newest-1.x pointer). All three joined docs-agents.test.ts's hand-written literal path contract and the generated _headers. Two absorbed consequences: the skill header states the schema version so its test stopped asserting a three-part shape, and src/version.ts gained PROTOCOL_VERSION/SCHEMA_VERSION beside VERSION, which changed the committed browser audit bundle (rebuilt). Verified: 3,377 bun tests, typecheck, all seven check:* gates, audit:registry clean, 2,985 axe cases (up 8 \u2014 the new page in light+dark across the exemption-free sweep), layout-lint re-recorded at 186 pages with every ratcheted count still 0, docs-switcher green. PRE-EXISTING, NOT CAUSED HERE: a handful of visual-matrix baselines are stale locally (they are git-ignored and CI seeds them; pattern__auth-form__document__light__ltr reproduces on a clean tree), and `faqir <cmd> --json` subprocesses in tests/commands/json-output.test.ts intermittently spin at 100% CPU when spawned from the full-suite process \u2014 observed on a clean tree before any edit in this session, and green when the file runs alone."
    },
    {
      "id": "0.9-01",
      "completed": "2026-07-28",
      "commit": "cedd48b",
      "summary": "The measurement, and the parity hole that hid it. `src/utils/layout-lint.ts` reduces \"cramped\" to four conditions over rectangles \u2014 gutter, seam, bleed, overlap \u2014 pure and `node:*`-free so 28 `bun test` cases drive it from literal boxes in both directions while `tests/visual/layout-lint.pw.ts` drives the same code over all 180 generated docs pages at 1280\u00d7900. Committed budget: 79 zero-gutter, 42 seam-bearing pages, 183 seams, 7 bleeds, 3 overlaps. It disagrees with the phase header's estimate in three places and each is argued rather than reconciled: a centred measure column IS a gutter (so `document`/`form-page`/`wizard` count, 79 not 84); only stacked BLOCK demos can seam (a row of badges is 0.9-08's problem, 183 not 187); and bleed asks reachability, not clipping \u2014 122 long `<code>` lines behind a live scrollbar are not findings (counting them would put a floor under a number 0.9-11 must drive to zero), which also means `carousel`'s eight slides are a gutter finding rather than a bleed one, while seven boxes at x=\u22124 inside `sidebar`/`dashboard-shell` are genuinely unreachable and are. A ratchet, not a wall: `compareBudget` fails a rise naming the pages responsible, passes a fall as printed slack, and `lint:layout:update` refuses to record a rise, so the file can only ever describe a better site. Then the parity fix: the visual harness's `body { padding: 24px }` AND its `main.vr-root` flex column with `gap: 16px` are gone \u2014 the second was the same lie in a more damaging form, since a 16px gap between demos is exactly what a seam hunter must not add \u2014 leaving a document that injects no CSS at all, pinned by a new `matrix.test.ts` case. Everything re-baselined: `test:visual` green as a whole at 4 243 tests, which resolves 0.8-14; its \"non-default theme cells\" half turned out to be a partial LOCAL set (the baselines are git-ignored, CI seeds them per \u00a712.2), documented in tests/visual/README.md. Telling the truth cost two real defects: with `align-items: flex-start` gone the scroll containers finally scroll, so the 390px axe sweep went red on `document`'s table root and `grid`'s `[data-scroll]` strip \u2014 both fixed at source with `tabindex=\"0\"`, the systemic remainder appended to 0.8-16, which had predicted it as \"content luck\"."
    },
    {
      "id": "0.8-12",
      "completed": "2026-07-27",
      "commit": "e653f59",
      "summary": "One module, five surfaces. `src/utils/layout.ts` holds the doctrine as data \u2014 three mechanisms, five primitives, the measure/rhythm token NAMES, the archetype list \u2014 and deliberately nothing a manifest, a token file or `breakpoints.ts` already knows, so README, `docs/layout.md`, the skill's generated Layout System section, context's `layout`+`responsive` blocks and the site's new Layout page read one source instead of keeping four copies in step. `docs/layout.md` is executable documentation: five archetypes (dashboard, landing, prose/document, split view, centred form), each parsed back out by `parseArchetypes()`, each audited to zero findings at every severity, and the docs site renders `esc(a.html)` of exactly what the test audited \u2014 the shipped page and the proven markup are the same bytes. The README half runs 0.8-10's rule backwards, per primitive and in both directions: nothing documented that the manifest does not declare, nothing declared that the subsection omits (which caught `data-variant` going unmentioned after stack's v2.0 direction move), with the ladder rows compared against `BREAKPOINT_LIST` rather than eyeballed. llms.txt now STATES the ladder and grammar rather than linking them \u2014 asserted on the hosted file through 0.7-15's byte-equality harness, so faqir.dev/llms.txt teaches layout to a crawler that reads nothing else. Writing the examples found two real defects, both fixed here rather than written around: `valid-variant` misread the declared prop `data-align-text` as `data-align` at a tier called \"text\" (stack ships that collision; `declaredAttributes()` short-circuits the suffix reader, pinned by a rules test), and `data-span` turned out to be a non-responsive prop, so `data-span-lg` audits clean and silently does nothing \u2014 the archetype uses `data-span=\"full\"`, the constraint is documented, and the modelling gap is filed as 0.8-17. 3092 tests green (+30), `check:skill`/`check:docs`/`audit:registry` clean, both changed site pages axe-clean in light and dark."
    },
    {
      "id": "0.8-11",
      "completed": "2026-07-27",
      "commit": "6798a17",
      "summary": "The viewport axis is the same generated-matrix trick applied to width: `discoverLayoutBearing()` filters the main matrix's components by the sibling manifest's `category === \"layout\" || kind === \"pattern\"` (11 + 15 = 26 today) and crosses them with 390/768/1280 in default\u00b7light\u00b7ltr \u2014 78 captures, not the 3 744 a \u00d712 themes \u00d72 schemes \u00d72 dirs sweep would have cost for information the single-viewport matrix already carries. Discovery is proven as a property, not a count: the meta-test re-derives the set from every manifest on disk and then hands the builder a synthetic component that enters via the category clause, enters via the layer clause, and stays out under `category: \"forms\"` \u2014 zero suite edits in all three directions. Every capture pre-asserts IN PAGE first (the 0.7-11 rule): `gatherLayoutFacts` reads the computed display + track count of every `[data-ui=\"grid\"]`, the drawer's position and the shell's column count, and the panes' visibility; `checkLayoutFacts` \u2014 pure, so all 19 of its branches are `bun test`-reproducible from literals \u2014 compares them to the ladder the MARKUP authored resolved through 0.8-01's canon, never to the stylesheet, so a sheet that stopped implementing its own ladder fails instead of agreeing with itself. Three browser cases inject an `!important` override that breaks one real rule and assert the check catches it, then re-mount unbroken and assert silence. The mobile axe sweep (26 \u00d7 {default,contrast} \u00d7 {light,dark} = 104 scans at 390 px, same pipeline, same imported discovery function, same single exemption list) earned its keep on the first run: `scrollable-region-focusable` on stats-dashboard's five-column table, invisible at 1280 px because `[data-ui=\"table\"]`'s `overflow-x: auto` only actually scrolls when narrow. Fixed at the source with `tabindex=\"0\"` on both roots \u2014 re-verified pixel-neutral against pre-fix baselines \u2014 not exempted; \"zero new exemptions\" is now structural, since an exemption entry has no viewport dimension and none of the three waived components is even in the mobile set. The general table contract (16 untouched roots across table/crud-table/document) filed as 0.8-16. 3062 pass / 0 fail (+25); playwright 4 235 visual and 660 a11y green."
    },
    {
      "id": "0.8-10",
      "completed": "2026-07-27",
      "commit": "87e6342",
      "summary": "undeclared-attribute + breakpoint-canon shipped as CSS_RULES (src/audit/css-rules.ts) \u2014 not ALL_RULES, which is typed over parsed markup: these read a stylesheet against a manifest, the same input pair the logical-properties family uses. One export feeds four consumers (rule inventory, faqir audit, registry gates 5+6, and FaqirAudit.auditComponentCss in the rebuilt browser bundle), so parity is structural; it is proven on 92 stylesheet fixtures \u2014 all 86 registry sheets paired with their own manifests plus six seeded/degenerate ones \u2014 through the committed bundle in node:vm. Getting the registry to zero was the real work: the rule found 40 undeclared attributes across 12 components (four times the ten layout had shipped), including two protocol data-size attributes and twenty on table alone, all now declared by 0.7-20's rule \u2014 controller-written becomes a state, authored becomes a prop. Twelve minor bumps, all breaking:false; no CSS or markup moved, so no visual baseline did either, and the bindings gained LButton.full, LText.done/truncate/tabular and size unions on LEmptyState/LSeparator. breakpoint-canon was already at zero \u2014 0.8-08/09 had done that work \u2014 so its proof is the seeded violation: grid's md floor flipped to max-width and its lg floor nudged to 65rem each report exactly once. Follow-up 0.7-20 resolved; the value-level sibling gap (21 selected values outside their group across 4 components, three of them two axes sharing one attribute) filed as 0.8-15 rather than guessed at. 3037 pass / 0 fail (+38)."
    },
    {
      "id": "0.8-09",
      "completed": "2026-07-27",
      "commit": "3c4e190",
      "summary": "patterns onto the breakpoint canon, mobile-first: fifteen max-width media blocks over four ad-hoc values (480/640/768/1024) became fifteen canon min-width floors over three (40/48/64rem), and every unconditional rule in the nine sheets is now the phone case. Two floors move, so both are breaking: inbox's pane collapse 640 -> md (2.0.0) and auth-form's full bleed 480 -> sm (2.0.0, the plan's 'no xs tier' position, verified in Chromium at 600/640 x light/dark); the other seven keep their values and are minor. dashboard-shell inverts hardest \u2014 the drawer is now the base and md carries four transform resets, one per specificity the base declares at \u2014 and its two :has(sidebar collapsed) rules moved into the md block, fixing a pre-canon bug where (0,3,0) outranked the single-column rule at (0,1,0) and a collapsed sidebar on a phone produced a 4rem column no grid area was placed in. document's tier block sits BEFORE @media print because a min-width query is true on paper. Equivalence proved by pixel: all 18 pattern x scheme renders at the matrix viewport are byte-identical before and after, so no committed visual baseline moves. The canon guard in tests/responsive-canon.test.ts widened from primitives+recipes to the whole registry; new tests/visual/responsive.pw.ts spot-checks the drawer and pane swap in Chromium at 390/768/1280. 2999 pass / 0 fail (+19), plus 8 Playwright."
    },
    {
      "id": "0.8-08",
      "completed": "2026-07-27",
      "commit": "525e6e7",
      "summary": "input, stepper and table moved onto the breakpoint canon mobile-first: three max-width blocks became canon min-width floors, and table's hide-below inverted from hide-below-X to hidden-by-default-revealed-from-X with the three specificity facts (base scoped to [data-responsive], reveals before the stacked block, :not([data-col-hidden])) that keep it behaviour-identical. Only sm shifts (30rem/480px to 40rem/640px) so table is 3.0.0 breaking:true; stack-below/hide-below are now declared enum props over canon tier names. STACK_BREAKPOINTS is a literal because build-core strips controller imports, so equality is enforced from outside against five files. New tests/responsive-canon.test.ts (28 tests) sweeps 71 sheets / 78 preludes / 25 width preludes; css-cascade gained resolveDeep for descendant selectors without touching existing paths. Size gate byte-identical: 146676 min / 43992 gzip before and after, 43.07 KB of a 44 KB budget. Visual: six failures found, all reproduced on a clean tree, filed as 0.8-14. 2980 pass / 0 fail (+28)."
    },
    {
      "id": "0.8-07",
      "completed": "2026-07-27",
      "commit": "295c133",
      "summary": "spacing scale extended to --space-64 with four page-rhythm steps (32/40/48/64) and four composed rhythm aliases (--section-gap-{sm,md,lg} over --space-{12,20,32}, --content-gutter over --space-6); density re-declares all of them in both blocks and the drift test's DERIVED dependent list picked the aliases up with zero edits - verified by a new assertion rather than hand-extended. Proven with the 0.7-11 probe-rule method at base and compact, invariants held in computed text. THE RULE GAP, ANSWERED: token-exists is a PROJECT rule (checkTokens walks <outputDir> from .faqir/config.json's installed list) and scripts/registry-audit.mjs ran three gates, none about tokens - the registry was never an input, so a project installing settings-page would have been warned and the framework's own source of truth never was. All three hypotheses pinned as tests (extractor does see var(--x, fallback); no documented skip matches space-48; checkTokens' source has installed.primitives + join(outputDir, and 'registry' nowhere). Fixed by findDanglingTokenReferences + Gate 4 sweeping 101 stylesheets against 298 tokens, predicate fully derived with no allowlist: token, OR knob declared in the same sheet, OR author knob with a fallback outside the token vocabulary - a fallback deliberately does NOT excuse a name inside a defined family. Found two more of the species beyond the one the task named: --text-tabular-width and --z-tooltip, both now declared on their component root (behaviour-identical). 2952 pass / 0 fail (+29); typecheck and all seven check:* gates green."
    },
    {
      "id": "0.8-06",
      "completed": "2026-07-27",
      "commit": "bba6f13",
      "summary": "container + measure tokens; the docs site's most-repeated escape retired. container is the centred measure column three call sites had already hand-rolled: max-inline-size from a shared token ladder, margin-inline: auto, optional inline gutter. data-measure (narrow|content|wide|prose|full) is a responsive variant group with tier precedence encoded in SPECIFICITY at birth (base (0,2,0) -> xl (0,6,0)), proved against shipped/reversed/selector-sorted rule orders; data-gutter is an enum prop over spacing steps 0-16 and deliberately NOT responsive (asserted: no data-gutter-<tier> anywhere) because a gutter restates what the padding parent already says, and its declared default of 0 is made real - a bare container resolves no padding-inline, which is what lets it nest in the docs shell without doubling the inset. The accepted name collision is enforced where it can be: the manifest description disambiguates container from container-type, and the suite asserts the sheet contains neither container-type nor @container - switcher (0.8-05) remains the only self-measuring component. Four tokens in aliases.css: --measure-narrow 32rem, --measure-content 48rem, --measure-wide 72rem, --measure-prose 65ch. IN-SESSION DECISION the task left open: surface's data-max is RECONCILED, not preserved - surface goes 2.0.0 with a breaking entry. Its 1.x ladder was five hardcoded px widths named sm|md|lg|xl|2xl, the breakpoint canon's own names used for max-widths, so data-max='lg' read as 'at the lg tier' when it meant '1024px wide' - exactly the multi-ladder drift 0.8-01 killed. Preserving those values would have needed a second token ladder under a second set of names; instead data-max now speaks container's five-value vocabulary over the same tokens. It cost nothing: [data-ui='surface'][data-max] had ZERO consumers in the repo, so no markup moved and no visual baseline shifted. A token test asserts --measure-<tier> may never exist for any canon tier, so the ladders cannot converge again. data-max and data-align-text were both in surface's CSS but absent from its manifest (the 0.7-20 gap) and are now declared props. form-page and wizard re-based on var(--measure-narrow), markup unchanged. docs.ts swaps the prose escape for <div data-ui='container' data-measure='wide'><article data-ui='prose'> - one edit, 94 pages; verified three ways (no style attribute containing max-width on ANY emitted page; every shell page carries the wrapper and no 72rem; the bundled stylesheet actually ships container's rules and --measure-wide, since deleting an escape without shipping its replacement would otherwise pass). Site-wide both style=\"max-width and max-width: 72rem are now zero across all 300 files. Two recorded behaviour changes: the cap is logical (max-inline-size) so it flips under RTL, and the prose column is genuinely centred rather than left-aligned in its flex parent - centring is the point of the swap. SCOPE: the task named two escape sites; only the first is a measure escape. The second (theme-preview frame's <main style='padding; display:grid; gap'>) is a padding/grid escape whose honest replacement is grid plus a gutter, not container, and swapping it would move twelve visual baselines - filed as follow-up 0.8-13 rather than silently absorbed. New suite tests/primitives/container.test.ts (36), completeness both ways. Regenerated: bindings (LContainer, one LContainerMeasure union reused by four tier props), skill, registry-index (85->86), all 12 theme manifests (the new tokens enter tokens_inherited), schema refs, codegen snapshots, docs site (297->300 files). bun test 2923 pass / 0 fail (+36 from a 2887 baseline); typecheck, audit:registry, check:bindings, check:skill, check:registry-index, check:schema-refs, check:audit-browser, check:docs all green. No new dependencies. FINDING (pre-existing, not caused by this task): tests/build/core-package.test.ts runs `bun run build:core-package` inside the suite, which rewrites the COMMITTED packages/core/cdn.json and rebuilds the git-ignored packages/core/dist. Because tests/generator/docs-agents.test.ts reads cdn.json at module-import time and compares it to dist/sri.json, the pair is order-dependent - the observed baseline flake (2886/1 vs 2887/0 on identical trees). cdn.json is deliberately NOT in this commit: its hashes pin the copy-for-agents snippets to what is actually published at @faqir-ui/core@0.2.4 on jsdelivr, and republishing hashes computed from an unreleased working tree would ship snippets the browser refuses to load."
    },
    {
      "id": "0.8-05",
      "completed": "2026-07-27",
      "commit": "e05791a",
      "summary": "cluster + switcher \u2014 one primitive per doctrine rung. cluster is rung 1 made concrete: a wrapping inline row whose responsiveness is INTRINSIC (flex-wrap needs no threshold), the tag-row/button-row/meta-row workhorse the registry had been faking; gap (0-16), align (incl. baseline) and justify are responsive variant groups with tier precedence encoded in SPECIFICITY at birth (base (0,2,0) -> xl (0,6,0), proved against shipped/reversed/selector-sorted rule orders), plus a data-push boolean prop on a CHILD for the actions-at-the-end idiom, margin-inline-start so it is the far end in RTL too. switcher is rung 2 and the first layout component with ZERO viewport coupling: container-type: inline-size + four @container blocks, the second container-query precedent after table. Because @container styles a container's DESCENDANTS and never the container itself, the flip cannot be flex-direction on the root \u2014 it is flex-basis 100% -> 0 on the children, at (0,2,0) over the base's (0,1,0). In-session decision recorded: data-threshold names the CANON tiers (sm 40rem / md 48rem / lg 64rem / xl 80rem) measured on the component's own inline size, NOT aliases of 0.8-06's measure tokens \u2014 a container prelude cannot read a custom property, so aliasing would have duplicated the numbers under a second name and re-created the multi-ladder drift 0.8-01 killed; the preludes are rebuilt in the test from containerQuery(tier, 'faqir-switcher') so they cannot drift. The declared default sm is made real by :not([data-threshold]) sharing the sm block at equal weight; inline-size: 100% is load-bearing (an unsized inline-size container in a shrink-to-fit parent resolves to zero width and every threshold reads 'narrow'). gap is deliberately non-responsive on switcher. The no-@media promise is asserted structurally (no @media/max-width anywhere, every rule media: null), by prelude, and behaviourally (each container-width case repeated at 390/640.5/1024/1920px viewports). Shared cascade helper grew a second axis: @container collection, child-targeted resolution via a ResolveContext (resolveIn/resolveValue), and :not() as a negated condition of equal weight \u2014 resolve/resolveRule keep their signatures and grid/stack/stats-dashboard are unchanged. Docs site's wrap use cases migrated: both rows of all twelve theme-preview frames and the gallery's theme + scheme switchers (which also shed the deprecated data-variant=\"horizontal\"); stack's suite keeps the no-escape claim, cluster's suite owns the positive one. New suites: tests/primitives/cluster.test.ts (26) + switcher.test.ts (22), completeness both ways for each. Bindings (LCluster with 12 tier props reusing 3 base unions + push?: boolean; LSwitcher with two plain unions and NO tier props), skill (41 primitives), registry-index (85 components) and the docs site (297 files) regenerated; codegen snapshots +113. bun test 2887 pass / 0 fail (+48); typecheck, check:bindings, check:skill, check:registry-index, check:schema-refs, check:audit-browser, check:docs, audit:registry (86 pages) all green; axe 12/12 on the new pages. No new dependencies."
    },
    {
      "id": "0.8-04",
      "completed": "2026-07-27",
      "commit": "b9f239a",
      "summary": "grid 2.0 \u2014 mobile-first, intrinsic auto mode, spans declared. The three 1.x defects each die with a named regression: the 640/641 range pair's fractional dead zone (640.5px now lands in the sm tier \u2014 min-width floors on the canon), the forced \u2264640 collapse (the blanket [data-cols]/[data-span] presence selectors are gone; unsuffixed data-cols holds at every width, spans are never crushed, and collapsing is authored: data-cols=\"1\" data-cols-md=\"2\" data-cols-lg=\"4\"), and source-order dependence \u2014 the one the task demanded beyond stack's 0.8-03 precedent. Tier precedence is encoded in SPECIFICITY: each tier block repeats [data-ui=\"grid\"] once more than the tier below (base (0,2,0) \u2192 xl (0,6,0)), so a bundler that reorders or splits blocks cannot change which tier wins; asserted structurally per selector and behaviorally by resolving five probe ladders identically against the shipped, reversed, and selector-sorted rule orders. The cascade machinery moved to a new shared pure-function helper (tests/helpers/css-cascade.ts, generalized from stack.test.ts: component name a parameter, viewport per call instead of a window.matchMedia mock \u2014 which is what makes the reorder proof possible), consumed by grid.test.ts (28 tests) and by stats-dashboard's new test resolving the authored metrics attributes through grid.css itself (1\u21922\u21924 incl. the dead zone; suite 25\u219226). Feature: data-cols=\"auto\" \u2192 repeat(auto-fit, minmax(min(100%, var(--grid-min)), 1fr)); data-min is an enum prop over rem steps 8|12|16|20|24 whose rules only re-declare --grid-min, the 16rem default sitting on the base rule at (0,1,0) so the override is specificity-safe too; auto is a full cols value so every tier accepts it; the media-free promise asserted the strong way (the WINNING rule at every probe width has media: null). Declared everything: cols/gap responsive (gap now 0\u201316 matching stack; declared default honestly 4\u21920 \u2014 no bare-grid gap rule ever existed; previews' hitherto-dead data-gap=\"3\" now resolves), span and scroll under props \u2014 scroll keeps its 0.x phone-strip meaning re-expressed mobile-first (snap strip below sm, restored to a grid from sm up by the boosted tier rules). Manifest 2.0.0 breaking entry with both migration recipes. Grid-vocabulary consumers updated in the commit: stats-dashboard's two KPI rows + manifest template/slot description, dashboard-shell's two content grids, stat.html's example row \u2014 all pixel-identical at the capture viewport, so visual baselines survive; pricing/feature-grid/key-value only borrowed the attribute NAMES for their own vocabularies and are 0.8-09's sweep. Bindings regenerated (LGrid: auto in the union, colsSm\u2026Xl/gapSm\u2026Xl reusing base unions, scroll?: boolean; codegen snapshots updated), skill + registry-index regenerated. bun test 2839 pass / 0 fail (+29); typecheck, audit:registry (84 pages), check:bindings, check:skill, check:registry-index, check:schema-refs, check:audit-browser, check:docs (291 files) all green. No new dependencies."
    },
    {
      "id": "0.8-03",
      "completed": "2026-07-27",
      "commit": "80d6683",
      "summary": "stack 2.0 \u2014 the full flexbox surface, declared. Six attributes and three value sets that stack.css had always shipped were invisible to every manifest-driven surface; all are now declared. Direction is the design decision: it rode `data-variant`, which the tier grammar may not suffix (P1, frozen), so `data-direction` becomes canonical and responsive while `data-variant=\"horizontal\"` stays as a deprecated 0.x alias selecting literally the same declaration block (asserted as one block, not merely the same outcome) \u2014 recorded as a `breaking: true` 2.0.0 changelog entry for 1.0-03 to pick up. gap (now 0..16), align (now incl. baseline), justify and direction are responsive variant groups; wrap, align-text, child flex and the deprecated `responsive` land under `props`. The bespoke `[data-responsive]` max-width collapse is re-expressed mobile-first on the canon and kept working as an alias, with `:where()` holding it at zero specificity so an explicit tier always wins \u2014 the sheet is now 4 ascending min-width blocks and zero max-width. 0.7-20's boolean-switch question answered: NOT a state (runtime vocabulary; data-wrap is authored and static) and NOT a variant (a closed value set with a default; data-wrap is valueless presence and would have had to become data-wrap=\"wrap\", breaking every page writing it bare) \u2014 `props`, schema-formal since 0.8-02. One generic generator consequence: a boolean prop declaring an explicit `attr` now emits the presence shape a state does in src/bindings/ir.ts, so LStack gains `wrap?: boolean` with no emitter/runtime/spec-shape change and zero churn elsewhere (no other boolean prop in the registry declares an attr). Tests (tests/primitives/stack.test.ts, 26): completeness BOTH ways (nothing selected-but-undeclared, nothing declared-but-unselected) as the per-component forerunner of 0.8-10; a real cascade resolver over the parsed sheet \u2014 specificity, then document order, media filtered through a matchMedia mock \u2014 proving direction flips at md, tiers hand over to each other, gap tiers override mobile-first, and both deprecated spellings still resolve; and the generated surfaces (IR tier expansion, both committed LStack modules, the docs Responsive column, and the ABSENCE of `flex-wrap` in all twelve theme frames \u2014 the 0.7-20 payoff). bun test 2810 pass / 0 fail (+26); typecheck, check:bindings (zero drift), check:skill, check:registry-index, check:schema-refs, check:audit-browser, check:docs (291 files), audit:registry all green. Deliberately unchanged: stack.html renders pixel-identically (attribute rename only), so the four committed visual baselines stay valid."
    },
    {
      "id": "0.8-02",
      "completed": "2026-07-27",
      "commit": "847116e",
      "summary": "Manifest schema gains `props` and `responsive`, and the category vocabulary closes. `props` was de-facto \u2014 134 entries across 59 manifests, read by recipe-ir/icons, absent from the published schema; it is now a `prop` definition (type \u2208 string|boolean|number|enum + description required; default/attr/values optional) derived from the shapes actually shipped, plus `ManifestProp`/`Manifest.props` in TS, which let recipe-ir.ts delete the local cast it used to reach a field the type system didn't know. `variant.responsive: true` means every declared value is also accepted as `data-<attr>-<tier>` for the 0.8-01 canon tiers, and FOUR surfaces consume that one declaration generically: bindings expand `cols` \u2192 `colsSm/colsMd/colsLg/colsXl` (ir.ts for primitives; a post-pass over the finished render tree in recipe-ir.ts for recipes, so both registration paths share one implementation), with tier props REUSING the base group's union \u2014 one exported type per group, not one per prop; the docs variant matrix grows a Responsive column; the skill grows the column plus a canon-quoting grammar line; context gains `responsive: {attr:[tiers]}` and the markdown/llms-full surfaces a `Responsive:` line from one formatter. Every surface is tested in both directions \u2014 a component declaring nothing gains no column/key/line \u2014 which is why 82 docs pages, the committed skill and the bindings stayed byte-identical (check:bindings zero drift) even though the generators changed. `valid-variant` learns the suffix grammar: wrong value and unknown tier are separate findings, reported independently, and a suffixed attribute of a NON-responsive group is deliberately ignored (0.8-10's rule, not this one's). No registry component declares a responsive group yet \u2014 0.8-03/0.8-04 are first \u2014 so the contract is proven against probe manifests and a scratch-registry probe recipe BEFORE the CSS lands, not after. Category: `form`(3)\u2192`forms`(17), `marketing`(4) documented, closed 11-value enum shared by schema + MANIFEST_CATEGORIES + CONTRIBUTING, with a drift test that parses the vocabulary back out of CONTRIBUTING's prose; `custom` is in the enum for `faqir create` and excluded from the registry by test. bun run test 2784 pass / 0 fail (+33); typecheck, check:bindings, check:skill, check:registry-index, check:audit-browser, check:schema-refs (95), check:docs (291 files), audit:registry all green."
    },
    {
      "id": "0.7-15",
      "completed": "2026-07-25",
      "commit": "913300d",
      "summary": "Docs site agent surfaces + hosting artifacts. Four machine files at stable root URLs (llms.txt, llms-full.txt, manifest.schema.json, registry-index.json) plus _headers and one copy-for-agents payload per component. The llms pair is the CLI's OWN generator pointed at the registry, not a second rendering: generateContext was split into a pure composeContextData and src/generator/registry-context.ts feeds it every registry manifest; the site's files are asserted byte-equal to formatContextLlms/formatContextLlmsFull over that data. Two forced changes: meta.scope (a hosted file may not say 'this project installs \u2026') and entries passed as [name,manifest] PAIRS instead of a Map, because empty-state ships in two layers and a name-keyed map drops one. Paths are the contract: tests/generator/docs-agents.test.ts spells every agent-facing path out as a LITERAL (deriving them from the generator's constants would only assert self-agreement) and a second case pins the constants to those literals. Schema + index copied byte-identical from the repo; schema served at the path its own $id claims (parsed, compared) and re-validated through the hosted bytes against all 83 manifests; index checked for faqir-registry-index@1, matching count, full coverage and a 64-hex hash per file. _headers is generated from the same list that emits the files, so nothing is served without a content type + CORS. Copy for agents: 83 standalone documents \u2014 reference fragment verbatim under the two-tag CDN preamble, EXACT version pin with matching SRI \u2014 asserted as documents in happy-dom (parses, exactly one link + one script both matching packages/core/cdn.json, no range pin, nothing relative, fragment byte-for-byte) and clean under DOCUMENT_RULES, inherited structurally because payload and example page mount through one mountFragment. The payload is three things from one source (visible <pre><code>, the file, the clipboard): the button copies textContent of the block it names, and the test clicks it in a DOM and compares the clipboard string to the snippet file, binding navigator explicitly to the window so it cannot pass against the test runner's. .html.txt not .html is deliberate \u2014 a payload, not a page \u2014 so 'no page the site serves reaches the network' stays absolute and no page class, audit gate, axe sweep or link rule needed an exception. SRI needed a hermetic source since packages/core/dist is git-ignored and absent in CI: build:core-package now also writes the COMMITTED packages/core/cdn.json, and the contract test fails while its version disagrees with packages/core/package.json. Checked and stated plainly: @faqir-ui/core is NOT published to npm yet, so the pinned URLs go live with 1.0-04. Deploy: bun run deploy:site (build:docs then `wrangler pages deploy`, no args \u2014 wrangler.toml carries name + pages_build_output_dir); wrangler via bunx, no new dependency; deployment stays a human step. Filed 0.7-21 (two `### empty-state` blocks in llms-full.txt share one anchor, so the pattern is unreachable by link). bun test 2735 pass / 0 fail (+28); check:docs 291 files / 4.25 MB; typecheck, check:audit-browser, check:skill, check:schema-refs, check:registry-index, audit:registry green; a11y 541 pass with only the four pre-existing tag-input failures of 0.7-16."
    },
    {
      "id": "0.7-14",
      "completed": "2026-07-25",
      "commit": "a932e9b",
      "summary": "Docs site: in-browser audit playground + theme gallery \u2014 the audit engine now runs CLIENT-SIDE with no server, and every theme is one swappable <link> away. To make the engine bundlable at all, auditHtmlSource moved to the new src/audit/html-audit.ts, whose entire import graph is free of node:* (checker.ts keeps node:fs for the on-disk sweep and re-exports the core, so there is still exactly ONE implementation and CLI\u2194browser parity is STRUCTURAL rather than re-implemented). scripts/build-audit-browser.mjs compiles src/audit/browser.ts to site/lib/faqir-audit.js \u2014 an IIFE installing one global, 28.76 KB raw / 9.76 KB gzip \u2014 COMMITTED like registry/core/faqir-core.js because the docs generator is pure/node:fs-only and copies files rather than invoking a bundler; `check:audit-browser` is its drift gate (CI + spawned from bun test) and it BIT twice during the session. Client-side proven three ways: the bundle audits inside a bare node:vm context with no DOM/require/module loader; it contains no node: specifier, no bundler runtime, no fetch/XHR/dynamic import and no http URL; and in Chromium against the served site every request is recorded and NOTHING leaves the origin. Manifests ship as a <script> assigning one global (311.67 KB, the whole registry verbatim \u2014 trimming would make parity a property of the serializer instead of the engine), so even file:// works. Parity: 513 shared fixtures (every site page, every reference fragment raw+sanitized, 150 fuzz-corpus samples, 14 dirty snippets) compared deep-equal, skipRules included, plus an assertion that the engine's audit-error fallback never fires. The one browser-only concern is manifest keying and it bit: empty-state ships as BOTH a primitive and a pattern, runAudit lets the last layer loaded win, so a first draft that sorted the payload keys audited those components against the wrong contract (27 fixtures disagreed) \u2014 payload is now written in load order, manifestMap does not re-sort, both halves regression-tested. Theme gallery: 12 themes (discovered, not listed), each with its own stylesheet, its own preview frame and a card described from its .theme.json; light+dark+auto. Instant switching asserted in Chromium by COMPUTED COLOUR: a scheme click flips data-theme on <html> and bg+fg both change; a theme click rewrites the href of one <link id=faqir-theme> and the page restyles into it; a value stashed on window survives both, proving no navigation. Scheme reaches all 12 frames at once by postMessage (frames are told, never reached into \u2014 file:// has no same-origin access to its own frames). That required splitting the stylesheet: styles/faqir.css is tokens+base+components and the theme is a SECOND link every page of the site carries, so the switcher is the site's own mechanism. The theme therefore loads one step later than faqir init concatenates it \u2014 safe in both directions and asserted, not assumed: no component CSS declares :root, and contrast.css (the only theme targeting components) already outranks them on specificity. One frame document per theme rather than ?theme=, so the theme is right before first paint (tested with all JS blocked) and every URL stays a relative path the link checker resolves. Third LAYOUT, not a third gate: frames/** are site pages under the same 100%-audit-clean + axe-clean gate (isShellPage / isFramePage name the layouts, isSitePage still names the gate) \u2014 and that gate bit HARD. Rendering a component in all 12 themes for the first time produced 15 axe contrast failures on badge's soft variants; reproduced statically with the framework's own checkThemeContrast (10 of 12 themes, 1.96:1 \u2192 4.50:1), root-caused to CONTRAST_TOKENS_RULE exempting the -subtle feedback pairs on the false premise that they carry no text, filed as 0.7-19 (with the baseline-regeneration caveat), and the frame now shows solid component colour + bare token swatches until it lands. Also filed 0.7-20: stack ships data-wrap in CSS but never declares it in its manifest, so nothing manifest-derived (docs site, skill, context, bindings) knows the attribute exists and no rule catches the gap \u2014 the frames wrap with an inline flex-wrap instead. Site JS is a closed named list on exactly three pages (playground, gallery, frame); all 83 component pages, the index, the token reference and the home page are still static HTML + stylesheets, held there by a test. Findings are built with createElement/textContent, never innerHTML; the preview frame is sandbox=allow-scripts (no allow-same-origin) so recipe controllers run but cannot reach the page. 45 new bun tests + 30 new axe pages; full suite 2707 pass / 0 fail (+45); build:docs 201 files / 3.22 MB, check:docs and check:audit-browser clean; typecheck, audit:registry, size, bindings, registry-index, skill and schema-ref gates green; docs-site axe gate 201/201 and the full a11y suite 539 pass with only the 4 pre-existing tag-input failures of 0.7-16. No new dependencies."
    },
    {
      "id": "0.7-13",
      "completed": "2026-07-25",
      "commit": "e301a76",
      "summary": "Docs site scaffold \u2014 site/ is a Faqir project with NO build step at runtime and only ONE hand-written page fragment (site/content/home.html) plus five strings in site.config.json; everything else is derived from manifests by the pure, node:fs-only generator src/generator/docs.ts (runs identically under bun test, the Playwright/Node runner, and scripts/build-docs.mjs). `bun run build:docs` \u2192 171 files / 2.61 MB: 86 site pages (home, component index, token reference, 83 component pages), 83 live-example pages, one stylesheet (tokens+base+theme+every component CSS, the faqir init cascade order), one engine copy. Shell = the dashboard-shell pattern; content = [data-ui=prose]; nav/link/badge/card/grid/separator/text/callout are the only other components \u2014 zero class attributes, zero own CSS, zero own JS. Per-component pages carry: live example, anatomy tree + slot table, variant matrix (every declared value as the attribute an author types), states, a11y table, token list linked by anchor into the token reference, templates, transforms, two-way composition links. The load-bearing design decision: TWO page classes with different gates, documented in docs/docs-site.md. Site pages must be 100% auditHtmlSource-clean at EVERY severity (they are) and axe-clean (172 scans, 86 pages \u00d7 light/dark, over real HTTP). examples/** wrap a registry reference fragment VERBATIM and are held to the document rules \u2014 the scope the registry self-audit already applies to that markup \u2014 plus controller-loaded, the one per-component rule that judges the wrapper (which is why recipe examples are live: they load registry/core/faqir-core.js). That split is content-derived, not a per-component escape hatch, and two tests prove the ground: every example body IS its registry fragment byte-for-byte, and every example component IS in the 0.4-24 a11y matrix. Probed and rejected the alternatives first: neither the reference fragments (365 findings) nor instantiated manifest templates (27/83 dirty) are clean under the per-component rules \u2014 filed as 0.7-17, which also affects `faqir audit` in user projects. Landmark placement is derived from the fragment, never from a name: own-<main> mounts as-is, dialog-class markup mounts BESIDE <main>, everything else is wrapped. Both gates bit and changed the site: <pre tabindex=0> (scrollable-region-focusable) and no <code> inside <a> \u2014 prose's code plate is --color-bg-muted with no colour, so --color-primary lands at 4.41:1 in dark (filed as 0.7-18; links now use the text primitive's mono variant). Also found: a JS-flipped data-theme samples mid-transition and reports phantom contrast failures, so the spec freezes transitions before the swap; and the sidebar must NOT carry data-state=expanded or dashboard-shell's 768px off-canvas drawer parks 83 links on top of the content on a phone. Layer-scoped page paths because empty-state is both a primitive and a pattern (the first draft's name-keyed map silently dropped one \u2014 now a regression test). Verified in Chromium against the served site: the dialog opens live inside the docs page's iframe (data-state=open, real overlay + focus trap) and the accordion goes collapsed\u2192expanded. 30 new bun tests + 173 axe cases; full suite 2662 pass / 0 fail (+30); typecheck, audit:registry, size, bindings, registry-index, skill and schema-ref gates green; the full a11y suite is green except the 4 pre-existing tag-input cases of 0.7-16, whose diagnosis this session corrected (the taglist is not empty \u2014 the role=combobox input inside role=list is the violation). No new dependencies."
    },
    {
      "id": "0.7-12",
      "completed": "2026-07-25",
      "commit": "6f367f9",
      "summary": "Dev overlay + Faqir.inspect + faqir-core.dev.js \u2014 TWO engine builds from ONE source. scripts/build-core.mjs now emits registry/core/faqir-core.js AND faqir-core.dev.js, resolved by three markers (exported+unit-tested applyDevMarkers): `/* @faqir:dev */` drops a line for production, `// @faqir:dev-start`\u2026`-end` drops a region, `// @faqir:dev-diagnostics` injects src/core-src/dev-diagnostics.js into the dev build only. Diagnostics reach the engine through ONE seam (`var devHooks = null`), so production carries neither the guards nor the messages; the byte-freeness gate DERIVES its string list from dev-diagnostics.js (comments stripped, literals \u226512 chars) instead of hand-listing, and runs against both the registry engine and the minified CDN artifact \u2014 plus `devReport`/`devSnippet`/`function isReorder` absent and exactly two surviving `devHooks` mentions. Warning-class policy made explicit: ERRORS stay in both builds (production terse, dev rich with the offending element's outerHTML), ADVISORIES are dev-only \u2014 so the unkeyed-l-for-reorder hint MOVED out of production along with its isReorder detector and per-update list snapshot (its three tests moved to the dev file; production kept parallel silence assertions), and two new advisories landed (unknown-directive, l-html unsanitized notice, each deduped per element). Faqir.inspect(el|selector) \u2192 { el, scopeRoot, scopeId, scope, directives, controller, state } SHIPS IN PRODUCTION (it is the agent surface; 1.0-02 types it): `scope` is a plain deep copy \u2014 magics excluded because they are non-enumerable by construction, functions/elements/cycles/depth collapse to markers, and reading registers NO reactive dependency; `directives` normalized (arg:null, modifiers:[]); `controller.api` is the identical object the controller returned; `state` reads the five protocol attributes ui-scoped exactly as $state/$variant do. window.__FAQIR_DEVTOOLS__ (version 1; keys version/dev/faqir/inspect/scopes/components/stores/warnings) is installed by both builds and mirrored as Faqir.devtools \u2014 the shared-realm problem the suite surfaced: with two builds loadable in one process only an instance-bound name is deterministic, so the global's identity is pinned against a freshly evaluated faqir-core.min.js. The overlay lives in src/dev/overlay.ts, bundled into the CLI and deliberately NOT in the registry so `faqir init` cannot copy it and `faqir bundle --js` cannot bundle it (both asserted, plus a needle-grep over the whole registry tree and dist); `faqir dev` serves it at /__faqir/devtools.js and rewrites HTML responses on the way out (disk untouched; --no-overlay opts out), proven end-to-end against a real spawned server over node:http (happy-dom's fetch enforces same-origin and refuses). Shadow-rooted, Ctrl/Cmd+Shift+F toggles, Escape closes; renders scopes with live data, components with protocol attributes and their OWN parts, and diagnostics. Verified in real Chrome against a faqir init project: all four diagnostic classes fire, the overlay toggles and renders, and the project's production engine matches none of the dev strings while still installing the handle. Size: engine+controllers 42.35 \u2192 43.08 KB gzip for inspect/devtools, gate 43 \u2192 44 KB (same precedent as 0.7-06/0.7-07); the dev engine is a new report-only target (budgetBytes:null \u2192 checkBudget returns reportOnly, cannot fail enforce at any size) printed at 44.03 KB gzip, with build:core also reporting 346.96 KB raw dev vs 340.18 KB production. Docs: new docs/devtools.md, README, CONTRIBUTING (seam table), AGENTS.md, the faqir-creator skill's references/directives.md, and a new `devtools` block in the generated project context (context.json + Markdown + llms.txt index + llms-full.txt tables). 94 new tests; full suite 2632 pass / 0 fail (+94); typecheck, audit:registry, size, bindings, registry-index, skill and schema-ref gates green. No new dependencies."
    },
    {
      "id": "0.7-11",
      "completed": "2026-07-25",
      "commit": "98a9ac6",
      "summary": "Density mode \u2014 `[data-density=\"compact\"|\"comfortable\"]` remaps the spacing scale and the control height ramp for a subtree, in ONE new file (registry/tokens/density.css, two rule blocks) with zero JS (build:core untouched at 29 controllers) and no sixth protocol attribute. The load-bearing CSS fact, documented in the file header and enforced by tests: a var() inside a custom property is substituted where the property is DECLARED, so a single :root multiplier cannot work \u2014 each density block re-declares --space-1\u2026--space-24 as calc(base * var(--density-scale)) (scale 0.75; --space-0/--space-px invariant), plus an EXPLICIT control ramp 28/32/40 rather than the multiplier (40\u00d70.75=30px is below a usable hit target), plus every :root alias whose value reads a remapped token (--card-padding, --field-gap, --callout-padding-*, --kv-*-gap, --button-height-*, --input-height) re-declared verbatim so it re-substitutes in scope. That dependent set is DERIVED from aliases.css+doc-aliases.css by a drift test, not hand-listed. document.css (--doc-*/--page-*) deliberately excluded: print density is a theme concern. Extension the task implied but the registry lacked: --control-height-{sm,md,lg} is now the canonical ramp in aliases.css with button/input aliasing it \u2014 surface grew by 3, so all 12 theme manifests regenerated; density.css itself is excluded from the themeable surface via a new shared isSurfaceTokenFile (replacing a copy-pasted `!== index.css` filter in 4 places) because a theme's :root cannot reach a subtree scope. Scoping proven in happy-dom by reading tokens the way a component does (a probe rule consuming them), since happy-dom does not expose inherited custom properties directly: descendants at any depth and the density element itself tighten, a sibling subtree does not, inner comfortable resets, compact-in-comfortable-in-compact still tightens, an unknown value is inert. Found en route: @faqir-ui/forms has emitted data-density from opts.density since 0.6-03 with no CSS behind it \u2014 now resolves, pinned by two tests; the generator stays outside the no-JS grep (it writes markup a human could type). New reference page registry/tokens/density.html (same form+table authored at both densities + a nesting block), deliberately NOT @ui:component so it stays out of the 84\u00d712 matrix; captured by its own spec over default/slate/soft \u00d7 {light,dark}, each capture first asserting IN PAGE that the compact button resolved shorter (a screenshot cannot go green on an inert attribute), and axe-scanned on the component gate's axes ({default,contrast}\u00d7{light,dark}, 4 scans, zero violations/exemptions). Caught in Chrome: the page used data-direction=horizontal on stack; the real variant is data-variant. Documented for agents in context.json (new `density` block + both prose renderers + the llms.txt index), the skill's references/tokens.md, and README. 31 new tests; full suite 2538 pass / 0 fail (+31); typecheck, audit:registry, size, bindings, registry-index, skill and schema-ref gates green; verified through a real faqir init in both concatenated and --tokens-split modes and in the CDN bundle."
    },
    {
      "id": "0.7-10",
      "completed": "2026-07-25",
      "commit": "4a79082",
      "summary": "Themes terminal + glass + soft \u2014 theme count 9\u219212 (plan said 'reaches 10'; document-serif had already landed out-of-band, noted as drift). All three are full 32-token themes (27 colors + 5 shadows) in :root/[data-theme=dark]/auto blocks. terminal: phosphor green (oklch hue 145, C\u22650.21) on near-black with amber warnings, glow shadows instead of depth, --font-sans aliased to var(--font-mono) so the whole UI is monospaced, every radius token AND component radius alias zeroed; dark-primary declared honestly \u2014 scheme:both backed by a real 'paper terminal' light scheme (dark-green ink on green-tinted paper), moods dark/terminal/technical/mono/retro. glass: authored fallback-first \u2014 every token solid, and ALL translucency (color-mix + backdrop-filter) lives inside one @supports ((-webkit-backdrop-filter\u2026) or (backdrop-filter\u2026)) block that frosts --card-bg / --glass-panel (color-mix over var(--color-bg\u2026), so one declaration follows both schemes) and applies blur to card/dialog-panel/sheet-panel/popover-content/toast via :root-boosted selectors (recipes hardcode background:var(--color-bg) at equal specificity). glass.test.ts documents and enforces the translucent-contrast computation: color-mix(C p%, transparent) \u2261 C at \u03b1=p/100 (premultiplied), composited channel-wise in gamma-encoded sRGB over EVERY opaque theme surface (bg/bg-subtle/bg-muted), fg+fg-muted \u2265 AA per scheme; plus the stripped fallback must clear the full standard pair list with every pair actually computed (no silent translucent skips), and comments are stripped before the structural assertions (a first-run bug: prose mentioning @supports ate the light block). soft: pastel consumer/health \u2014 warm cream/cocoa surfaces, calm low-chroma teal primary (C\u22640.12 is what reads as pastel; inks stay deep for AA), pill buttons/badges via radius-full, 1rem/1.5rem/2rem radius scale; moods pastel/friendly/consumer/health/calm/rounded, test-enforced fully disjoint from terminal's (the acceptance criterion). One contrast fix en route: glass dark destructive 0.60\u21920.58 L (white label was 4.39:1). All gates are glob-driven \u2014 coverage matrix, manifest consistency, contrast-tokens AA, and the visual matrix picked the three up with zero edits; 12-capture Playwright smoke green; all three previews (aurora-derived; glass gets radial-gradient blobs behind the panels so the frost is visible) browser-verified in both schemes. Manifests generated via SEED entries + gen:theme-manifests, $schema via gen:schema-refs. 71 new tests; full suite 2507 pass / 0 fail (+71); typecheck + audit:registry green. No JS, no size/bindings/skill cascade \u2014 themes are CSS+manifest only."
    },
    {
      "id": "0.7-09",
      "completed": "2026-07-24",
      "commit": "084f57b",
      "summary": "Patterns stats-dashboard + inbox \u2014 two composition-only patterns with ZERO JavaScript of their own (no controller file; build:core untouched at 29 controllers). stats-dashboard is a reporting page: the KPI row IS data-ui=grid (data-cols=4/data-cols-md=2), so 4->2->1 is grid.css and the pattern's sheet declares no metrics columns at all \u2014 the deliberate trade being that the stats inside carry no data-part (it would be an orphan part of the grid); report panels are cards; the report table is the table recipe with its full formatting contract (data-format on th repeated on every td, raw data-value, tfoot data-aggregate/data-col) and a test that adds up each aggregated column and compares it with the AUTHORED total. Corrected against table.js en route: aggregation is declared in the tfoot alone \u2014 a footer cell reads a whole column by index, body cells opt into nothing (the first draft's data-aggregate on body cells was removed from markup and manifest). Slot named `report` not `panel` because the audit's aria-describedby rule treats [data-part=panel] as a dialog's describable surface. inbox is a fully declarative list-detail view: two instances of the same markup on one page \u2014 authored rows, and l-source:messages + <template l-for l-key> for rows AND details \u2014 with selection in one l-data and everything else bound (:hidden, :data-state, :aria-current, l-text, @click); a directive-vocabulary test pins exactly which directives the page may use. Both instances are driven under real faqir-core with fetch stubbed: selection swaps the pane, aria-current + data-state mark the open row, back clears it, empty state renders when nothing is selected, the served JSON renders two rows/details, and a 503 surfaces the role=alert error line. Deviation: the reactive :data-state sits on the PANES, not the root, because faqir-core never applies bind directives on an l-data scope root (known follow-up 0.6-15) \u2014 the collapse CSS keys on [data-part=\u2026-pane][data-state=inactive] and the manifest says why. Responsive collapse is pure CSS at the registry's 640px breakpoint, tested through a matchMedia mock that reads the real rules out of inbox.css. Caught in Chrome and fixed with a regression test: an unscoped [data-ui=inbox] [data-part=list] rule was restyling the nested tabs' tablist. Both manifests carry a data_shape block (what an agent binds where) pinned by tests. 58 new tests; both pages audit-clean and axe-clean in real Chromium (8 scans, {default,contrast}x{light,dark}, zero exemptions), auto-swept by the visual matrix; verified in Chromium at 1280/900/420 px in both schemes. Cascade regenerated: registry-index (81->83 components), faqir-creator skill. Full suite 2436 pass / 0 fail (+58); typecheck, audit:registry, size, bindings, index/skill/schema-ref gates green."
    },
    {
      "id": "0.7-08",
      "completed": "2026-07-24",
      "commit": "26746d1",
      "summary": "Patterns pricing + landing kit (hero, feature-grid, site-footer) \u2014 four composition-only patterns with ZERO JavaScript (no controller file; build:core still 29 controllers, engine+controllers unchanged at 42.35 KB), and `faqir scaffold landing-page` promoted from ad-hoc synthesis to registry composition. The scaffold now READS the registry: src/scaffolds/landing.ts lifts the block between <!-- @ui:scaffold landing-page --> markers out of each pattern's own reference page and assembles hero+feature-grid+pricing inside <main> with site-footer as a sibling landmark \u2014 asserted byte-for-byte, so editing a pattern edits every future landing page; the old inline <style> block and ad-hoc [data-part=hero]/[data-part=cta] markup are gone. Design constraint discovered and documented in every manifest's composition.notes: a data-part belongs to its NEAREST data-ui ancestor, so (a) tier/item grids are pattern CSS rather than a nested data-ui=grid (which would orphan [data-part=tier]), (b) inside a pricing tier you address the CARD's slots, (c) a nested primitive must not carry both data-part and data-variant. pricing composes card tiers + stat for the price + badge + separator + icon ticks, highlight via data-state=featured on the tier (card.states is empty so valid-state skips it) plus an in-content 'Most popular' badge so emphasis is not colour-only. feature-grid's icon plate WRAPS data-ui=icon rather than being it \u2014 caught in Chrome: an icon paints its glyph with background-color:currentColor behind a mask, so a background on the icon element repaints the glyph. Fixed en route: cssLinks assumed <name>.css and emitted a 404 link for the icon primitive's icons.css; it now reads files.css from the manifest (pinned by a linked-stylesheet-exists test). ManifestState gained the applied_to/description fields the registry already used. 72 new tests; all four pages audit-clean (component + document rules) and axe-clean in real Chromium (16 scans, {default,contrast}x{light,dark}, zero exemptions), auto-swept by the visual matrix (asserted by meta-test), responsive behaviour asserted at CSS level on the registry's 1024/640 breakpoints, composed page verified in Chrome at 1280/900/420 px in both schemes. Cascade regenerated: registry-index (77->81 components), faqir-creator skill. Full suite 2378 pass / 0 fail (+72); typecheck, audit:registry, size, bindings and schema-ref gates green. Filed follow-up 0.7-16: recipe tag-input fails axe aria-required-children in all four gate cases \u2014 PRE-EXISTING (reproduced on a clean tree), from 0.7-06."
    },
    {
      "id": "0.7-07",
      "completed": "2026-07-24",
      "commit": "f3dfa08",
      "summary": "Recipe carousel \u2014 a CSS scroll-snap strip with a deliberately tiny enhancement layer. CSS does all the sliding (overflow-x:auto + scroll-snap-type:inline mandatory, slides scroll-snap-align:start, viewport tabindex=0), so the component is complete with JS disabled; every inert control ([data-part=controls], [data-part=dots]) ships `hidden` and is un-hidden on init and re-hidden by destroy(). The 1.40 KB gzip controller (stated budget 1.5 KB in the manifest's js_budget block, pinned by a test that reads the number from the manifest) adds only: prev/next + dot navigation, current-slide tracking by rAF-throttled scroll math (no IntersectionObserver), the loop-or-stop boundary contract (data-loop wraps; default disables the end button), a role=status live region announcing 'Slide N of M', faqir:change, and reduced-motion honoring via an explicit behavior:'instant'. Scrolling uses the physical delta slide.left-viewport.left added to scrollLeft (RTL-safe); only current-slide detection is direction-aware. 24 new tests. Verified in real Chrome against a faqir init+add project: button/dot targets land exactly on 604/1208 px, tracking follows user scrolls and mandatory-snap re-snapping, and the script-less page is a fully working focusable snap strip with no controls rendered. Cascade regenerated: build-core (29 controllers), registry-index (77 components), faqir-creator skill, vue+react bindings + snapshots; guard counts 28\u219229; engine+controllers budget 42\u219243 KB (measured 42.35). Full suite 2306 pass / 0 fail (+26); typecheck, audit, size and all drift gates green."
    },
    {
      "id": "0.7-06",
      "completed": "2026-07-24",
      "commit": "11e47a4",
      "summary": "Recipes tag-input + toggle-group. toggle-group: single/multi-select group over NATIVE radio/checkbox controls styled as toggle buttons, with a JS controller for roving tabindex (Arrow/Home/End, focus-follows-selection in single mode, disabled-skipping), Space/Enter toggling that funnels every mutation through a native change event, single-mode exclusivity for uncontrolled use, a [data-state] hook, and faqir:change \u2014 so l-model binds string (single) and array (multi) through the engine's native paths with ZERO core changes (proven by real Faqir.start() tests). Design note: multi uses native checkbox aria-checked, not button aria-pressed, precisely to make array l-model native. tag-input: controller composing the chip primitive (committed tags are real data-ui=chip markup; tag-input.css defines no chip rules) + combobox listbox behaviour (filter/highlight/empty suggestions) \u2014 type+Enter adds, Backspace-on-empty removes last, chip dismiss removes, case-insensitive duplicates policy (data-allow-duplicates opt-out); value array via getValue()/faqir:change and mirrored as JSON onto a hidden [data-part=value] input firing native input for l-model sync (real-engine round-trip test). 37 new recipe tests. Cascade regenerated: build-core (28 controllers), registry-index (76 components), faqir-creator skill, vue+react bindings + snapshots; guard lists bumped (controller-source-of-truth 26\u219228, auto-init 26\u219228); engine+controllers size budget 41\u219242 KB (measured 41.65) for two genuine recipes. Full suite 2280 pass / 0 fail (+43); typecheck, audit, size, and all drift gates green. Also fixed a pre-existing red baseline first (chore 1337bf6: stale react/vue index snapshots from 0.7-05's file-upload bindings)."
    },
    {
      "id": "phases v0.3\u2013v0.5",
      "completed": "2026-07-13",
      "commit": null,
      "summary": "Marked all v0.3, v0.4, v0.5 index tasks \u2705 per user confirmation that everything through v0.5 is implemented. Traversal now begins at v0.6."
    },
    {
      "id": "0.6-01",
      "completed": "2026-07-13",
      "commit": null,
      "summary": "Normalized field-group validation vocabulary to invalid|validating|disabled (breaking rename of error\u2192invalid), standardized the [data-part=required] marker, bumped manifest to 2.0.0 with a breaking changes entry, and proved the faqir upgrade migration path. 1853 tests green."
    },
    {
      "id": "0.6-02",
      "completed": "2026-07-13",
      "commit": null,
      "summary": "faqir-validate.js plugin: bare l-validate reflects native ValidityState + custom l-validate:<name> validators into the field-group contract (data-state=invalid, error part, aria-invalid/describedby); submit gating + on-valid hook + validate-on-submit-then-live policy. 1.26KB gzip, 16 tests."
    },
    {
      "id": "0.6-03",
      "completed": "2026-07-13",
      "commit": "683ba6e",
      "summary": "Added the zero-dependency @faqir-ui/forms scalar renderer with strict JSON Schema/UI schema validation; string, textarea, enum radio/select (four-option threshold), number/integer, boolean checkbox/switch, date-picker, email, and URI mappings; deterministic field-group wiring and required/description propagation. Eleven golden schemas are audit-clean, with Bun, Node, browser-target, typecheck, registry audit, package dry-run, and the full 1891-test suite green."
    },
    {
      "id": "0.6-05",
      "completed": "2026-07-15",
      "commit": "5a19569",
      "summary": "Added the zero-dependency faqir-persist and faqir-intersect plugins with reactive JSON persistence, private-mode fallback, namespaced magic access, enter/leave/once hooks, teardown cleanup, deterministic faqir bundle --js distribution, and generated context/skill discovery. Both plugins are under 1 KB gzip and the full 1905-test suite is green."
    },
    {
      "id": "0.6-06",
      "completed": "2026-07-15",
      "commit": "07a860d",
      "summary": "Added the zero-dependency faqir-mask plugin with pure caret-safe 9/a/* masking, paste normalization, masked-display/raw-model binding, teardown cleanup, and optional input-otp integration. The plugin is 1.96 KB gzip and the full 1917-test suite is green."
    },
    {
      "id": "0.6-07",
      "completed": "2026-07-15",
      "commit": "43ab335",
      "summary": "Added canonical document header/footer parts with native running-element margin boxes, Chromium fixed-position fallbacks, CSS page counters, tokenized print geometry, a three-page audit-clean reference, renderer support metadata, and regression coverage. The full 1928-test suite, typecheck, registry audit, generated checks, and targeted visual snapshot are green."
    },
    {
      "id": "0.6-08",
      "completed": "2026-07-16",
      "commit": "cba6f7d",
      "summary": "`faqir scaffold invoice` + `faqir scaffold report` \u2014 two ready-to-print, audit-clean document scaffolds in a new src/scaffolds/documents.ts (382 lines) behind an extended src/commands/scaffold.ts: invoice (key-value blocks, doc-table with currency formats + tfoot totals, qr-code payment block, signature, page-break) and report (running headers/footers, callouts, stats, description lists, image). Both render as two A4 pages in Chromium with the authored header/footer and `Page 1 / 2`\u2013`Page 2 / 2` counters repeating; `--theme <name>` overrides the `document` default and `faqir theme set` stays compatible, with bundle regeneration keeping theme/components in sync. Placeholder convention is `FAQIR_REPLACE: path.to.value` markers in both templates, documented in the scaffold output and README so agents know what to replace. Command tests (tests/commands/scaffold.test.ts, 173 lines) prove deterministic generation, zero findings from a full-project audit, and full document-layer component coverage; the generated pages join the Playwright axe + visual gates via tests/scaffolds/document-pages.ts with tests/a11y/scaffolds.pw.ts and tests/visual/scaffolds.pw.ts. En route the commit also touched callout.css, image.css, the table recipe (css/html/js), faqir-core.js, src/audit/checker.ts and the visual workflow. Reconstructed from git after the fact (the session that shipped it did not append a history entry); details taken from the commit diff and the acceptance-criteria annotations in FAQIR-PLAN.md, not from the original session.",
      "reconstructed": true
    },
    {
      "id": "0.6-09",
      "completed": "2026-07-16",
      "commit": "2d59926",
      "summary": "Documents: `watermark` primitive + `barcode` recipe + `document-serif` theme \u2014 the three pieces that closed the docs/for_craft.md wishlist (that file was rewritten in the same commit to say so). `watermark` is CSS-only (75 lines): repeated/diagonal fixed-position text that survives print media and never intercepts pointer events, both asserted in tests/primitives/watermark.test.ts. `barcode` follows the qr-code pattern \u2014 a pure-JS Code128 encoder (barcode.js, 156 lines) rendering SVG/CSS bars, with tests/recipes/barcode.test.ts checking checksum + bar patterns against known vectors and clean errors on out-of-charset input; it registers with the auto-init path (faqir-core.js) like every other recipe. `document-serif` is the contracts/legal theme: full token coverage (238-line manifest), preview page, and coverage-matrix/manifest-consistency tests, with scripts/gen-theme-manifests.mjs taught about it. Cascade regenerated: registry-index.json, faqir-creator skill (SKILL.md + primitives/recipes references) and README. Acceptance criteria were ticked in the follow-up plan commit 0841b9c, including the note that the barcode was scanned in practice. The plan's status cell was left \u2b1c until 2026-07-25. Reconstructed from git after the fact (the session that shipped it did not append a history entry); details taken from the commit diff and the acceptance-criteria annotations in FAQIR-PLAN.md, not from the original session.",
      "reconstructed": true
    },
    {
      "id": "0.6-10",
      "completed": "2026-07-16",
      "commit": "01f0654",
      "summary": "Print visual regression \u2014 the document layer's equivalent of 0.4-23. A dedicated playwright.print.config.ts + tests/visual/print/print.pw.ts render the reference documents (invoice and report scaffolds + the document pattern page) to PDF via headless Chromium, Poppler rasterizes them, and seven full-page 96-DPI PNGs are image-diffed against baselines at a 25-pixel maximum. Page counts are asserted before any pixel comparison (invoice = 2, report = 2, document pattern = 3) so pagination regressions fail cheaply, and the extracted PDF text is checked for the authored header/footer on every page plus the `Page 1 / 3`\u2013`Page 3 / 3` and `Page 1 / 2`\u2013`Page 2 / 2` counters. tests/visual/print/matrix.test.ts is a meta-test: it independently scans DOCUMENT_SCAFFOLDS and every manifest `files.print_reference` and compares that ground truth against the generated print matrix, so a new document-kind page cannot silently escape the gate. .github/workflows/print-visual.yml runs the Chromium/Poppler job path-filtered to document-layer, harness and workflow paths, uploading diff/PDF artifacts on failure; tests/visual/print/README.md documents local reproduction and how to bless intentional changes. The gate was proven by flipping `--page-margin` 15mm \u2192 16mm \u2014 all three cases failed with 12,031\u201331,642 changed pixels \u2014 then reverting. Reconstructed from git after the fact (the session that shipped it did not append a history entry); details taken from the commit diff and the acceptance-criteria annotations in FAQIR-PLAN.md, not from the original session.",
      "reconstructed": true
    },
    {
      "id": "0.6-11",
      "completed": "2026-07-16",
      "commit": "bee5d1f",
      "summary": "`faqir theme generate` \u2014 parametric OKLCH themes, landed across seven commits (5aa2bc1, 30cfa01, 1d3286c, e89e908, abf93bd, 424dc8c, bee5d1f) rather than one. A pure generator in src/commands/theme-generate.ts (658 lines) over an extended src/utils/oklch.ts derives an 11-step accent ramp from one brand colour (opaque OKLCH or short/full hex), maps semantic primary/hover/active/subtle through it, and computes dark-mode inversions; deterministic cool/warm/gray neutrals, sm/md/lg radii, light/dark/both schemes. Every contrast pair is verified BEFORE any filesystem write \u2014 light and dark pick contrast-safe inverted steps, with primary lightness auto-adjusted when needed or a clean failure with guidance. Emits themes/{name}.css + {name}.theme.json, and `--document` additionally emits a light-only, A4-aware, flat/zero-shadow, crisp-radius, pt-sized brand-matched print variant with its own derived manifest. `--json` (schema v1) reports normalized accent/options, every written path, all declared-scheme token pairs with ratios, pass state, and whether lightness was auto-adjusted. The MCP placeholder now exposes the same pure generator in memory, returning CSS/manifests/ratios with no filesystem access. tests/commands/theme-generate.test.ts (275 lines) runs the full shipped-theme gauntlet \u2014 coverage matrix, manifest consistency, contrast-tokens \u2014 programmatically over a five-hue matrix, plus determinism and dark-only generation. Verification at the time: 1,984 Bun tests, root/MCP/forms typecheck, Node CLI and MCP builds, plain-Node CLI generation, CLI smoke, registry self-audit and package dry-run all green (one transition-timing test flaked once, then passed in isolation and in the clean rerun). Reconstructed from git after the fact (the session that shipped it did not append a history entry); details taken from the commit diff and the acceptance-criteria annotations in FAQIR-PLAN.md, not from the original session.",
      "reconstructed": true
    },
    {
      "id": "0.6-12",
      "completed": "2026-07-16",
      "commit": "7b7cade",
      "summary": "User-requested out-of-order task: new `faqir bindings vue` command (target-agnostic manifest\u2192IR walker + Vue emitter) generating all 39 primitives as typed Vue 3 components over a 128-line hand-written runtime; typed literal-union props, data-part slot projection, boolean state props; drift guard in bun test + CI check:bindings. Full 2014-test suite green. NOTE: state.current was 0.6-04 (started, unfinished in a prior session) when this session began \u2014 0.6-04 remains \u2b1c and is the next pending task in document order."
    },
    {
      "id": "0.6-04",
      "completed": "2026-07-17",
      "commit": "0317fa9",
      "summary": "@faqir-ui/forms composites: nested objects \u2192 fieldset cards (recursive), enum arrays \u2192 checkbox-group/multi-select on the shared 4-value threshold, arrays of objects \u2192 repeatable groups (form-scope l-data + keyed l-for, static+bound row wiring, min/max gates), ui:groups layout groups, and a ui:wizard multi-step stub of the unlanded 0.6-14 pattern (stepper + card panels + per-step faqir-validate gate). All 18 golden schemas audit-clean; \u00a77.2 mapping checklist mirrored in tests; patient-intake e2e (render\u2192validate\u2192submit, zero custom JS) green under happy-dom. Forms now always emit l-data (bare forms were never walked by bootstrap); core scope-root findings filed as follow-up 0.6-15. Deviation: multi-select instead of tag-input (no tag-input in registry)."
    },
    {
      "id": "0.6-13",
      "completed": "2026-07-17",
      "commit": "8bca7a4",
      "summary": "@faqir-ui/vue recipes + SSR: `faqir bindings vue` now generates all 22 recipes \u2014 recipe IR (src/bindings/recipe-ir.ts) parses each manifest reference template into a static render tree (contract attrs + hidden FOUC guards verbatim, l-* stripped, unique parts \u2192 named slots, variants \u2192 union props on their applied_to part, placeholders \u2192 string/boolean props, id via Vue 3.5 useId) and scans controllers for @ui:provides methods + dispatched faqir:* events (cross-recipe imports followed); controllers vendored into packages/vue/src/controllers with rewritten imports under the drift guard. Hand-written recipe-runtime.ts (149 lines): onMounted create / onBeforeUnmount destroy, events re-emitted as Vue events (detail, event), controller API exposed (dialogRef.open()). 30 new tests incl. per-recipe lifecycle/leak spies, SSR renderToString in a zero-DOM subprocess, warning-free hydration for every recipe. Demo SFC (5 recipes + 3 primitives) verified in Chrome against the real CSS bundle; README covers recipes + Inertia/SSR. vue peer bumped to ^3.5.0. 2082 tests green."
    },
    {
      "id": "0.7-01",
      "completed": "2026-07-18",
      "commit": "5bd8e00",
      "summary": "@faqir-ui/react codegen + primitives. New `react` target on `faqir bindings <target>`, sharing the target-agnostic IR (src/bindings/ir.ts) with Vue \u2014 no forked manifest-walking. src/bindings/react.ts emits one spec-only TS module per primitive (literal-union variant types, boolean state props, named slots as ReactNode props, Props extending ComponentPropsWithoutRef<tag> with all Faqir-declared names Omitted from the base \u2014 size/title collide with DOM attrs). Hand-written packages/react/src/runtime.ts (130 lines) builds a forwardRef component via React.createElement: data-ui/variant attrs, first-truthy data-state, presence/aria states, data-part slot wrappers, ref\u2192root, non-Faqir prop fall-through. RSC-safe: no `use client`, no hooks, no react-dom import; renderToStaticMarkup renders every primitive server-side. 38 new tests (40 codegen snapshots; @testing-library/react DOM matrix + ref-forwarding over all 39 primitives; RSC smoke; positive tsc + negative union fixture; drift/stale guards). Dev deps react/react-dom 19 + @testing-library/react in packages/react (react/react-dom are peers ^18.2||^19; CLI stays zero-dep). check:bindings/gen:bindings/typecheck now cover both targets; CI drift gate extended. 2136 tests green (+38). Node dist CLI verified."
    },
    {
      "id": "0.6-14",
      "completed": "2026-07-17",
      "commit": "4a7b2ce",
      "summary": "Patterns wizard + form-page. `wizard`: fully-declarative multi-step form (stepper + card panels + field-group + buttons) driven by one l-data `{ step }` \u2014 :hidden/:data-state bindings for visibility+indicators, @click/submit nav, and faqir-validate's l-validate on-valid hook to advance/block-while-invalid (inactive-step controls :disabled so excluded from validation) and, on the final step, set data-state=submitted + dispatch faqir:wizard-complete. Zero custom JS; verified end-to-end in Chrome (next/back, completed states, gating, completion event detail.steps=2) and by 11 happy-dom tests. `form-page`: the canonical schema-rendered form page, pinned byte-for-byte to @faqir-ui/forms output \u2014 shared FORM_PAGE_SCHEMA fixture in cases.ts, pattern HTML generated from it, drift-guarded by packages/forms/tests/form-page-golden.test.ts (generator\u2194pattern can't diverge). Both manifests validate + document composition; both pages faqir-audit-clean and auto-discovered by the visual/a11y matrix. Regenerated registry-index + faqir-creator skill. 2098 tests green (+16)."
    },
    {
      "id": "0.7-02",
      "completed": "2026-07-18",
      "commit": "2449572",
      "summary": "@faqir-ui/react recipes + hooks. faqir bindings react now emits per-recipe use-client wrappers over the shared recipe IR: typed props (variant unions, string/bool props, named-part ReactNodes, on<Event> callbacks) + parsed render tree passed to the one new hand-written recipe-runtime.ts (createFaqirRecipe + useFaqirController). Runtime renders the static tree via createElement (SSR-safe; camelCased DOM attrs + slider inline-style string normalized), attaches the vendored controller in useEffect (StrictMode-safe via the controllers double-init guard cleared on destroy), forwards faqir:* events to on<Event> props (latest read at dispatch), exposes the API via useImperativeHandle. Controllers vendored verbatim under the drift guard. useFaqirController takes the controller factory (tree-shakeable) not a name string. 39 tests (22 recipe behaviors + codegen client-boundary/spec-only/vendor/drift). Browser-verified demo (7 recipes + 8 primitives) against the real CSS bundle, zero console errors. 2158 tests green (+22)."
    },
    {
      "id": "0.7-03",
      "completed": "2026-07-21",
      "commit": "6126c00",
      "summary": "Recipes `context-menu` + `menubar`, built on a shared menu-navigation core rather than a third copy of arrow-key logic: registry/core/menu-navigation.js (172 lines) was factored out and dropdown.js rewritten onto it (100 lines changed), with tests/build/menu-navigation-source.test.ts asserting the single implementation by module structure \u2014 not vibes \u2014 and tests/core/menu-navigation.test.ts (187 lines) covering the core directly. `context-menu` (104-line controller) opens at pointer coordinates on contextmenu, suppresses the native menu, and closes on Escape/outside-click with full keyboard nav; `menubar` (248 lines) implements the WAI menubar pattern \u2014 horizontal arrows across top level, vertical arrows into/through submenus, Escape closing submenu then returning menubar focus, roving tabindex \u2014 asserted by tests/recipes/menubar.test.ts (445 lines) and context-menu.test.ts (242 lines), with menu-audit.test.ts keeping both manifests audit-clean. Both recipes were mirrored into the vue and react packages (a shared _core-menu-navigation.ts on each side, controllers + recipe wrappers + regenerated codegen snapshots), and the engine/build-core/registry-index/skill/README cascade regenerated. The 24-controller core measured 36.37 KB gzip, so scripts/check-size.mjs was raised minimally 36 \u2192 37 KB (noted in the 0.4-29 follow-up row). 46 files, +4277/\u2212284. The plan's status cell was left \u2b1c until 2026-07-25 even though its acceptance criteria were ticked in the same commit. Reconstructed from git after the fact (the session that shipped it did not append a history entry); details taken from the commit diff and the acceptance-criteria annotations in FAQIR-PLAN.md, not from the original session.",
      "reconstructed": true
    },
    {
      "id": "0.7-04",
      "completed": "2026-07-21",
      "commit": "248cc8f",
      "summary": "Recipe `tree-view` \u2014 the WAI tree pattern in a 479-line controller: role=tree/treeitem/group, aria-expanded, aria-level/setsize/posinset maintained at every level, full keyboard traversal (arrows expand/collapse/traverse, Home/End, typeahead), single selection events, disabled items skipped in navigation, and a lazy-children hook that emits an expand event for app code or `l-source` to answer. tests/recipes/tree-view.test.ts (358 lines) drives the keyboard matrix over a 3-level fixture and round-trips the ARIA state; tree-view-reactive.test.ts (180 lines) is the keyed-`l-for` integration \u2014 trees being the stress case for the 0.3-05/06 reconciler \u2014 and tree-view-audit.test.ts keeps the reference page with its nested fixture audit-clean. Mirrored into vue and react (484-line controller each plus recipe wrappers and regenerated codegen snapshots); registry-index, skill references and README regenerated. The 25-controller core hit 38.20 KB gzip, so the size ceiling moved 37 \u2192 39 KB. 25 files, +3010/\u221220. Reconstructed from git after the fact (the session that shipped it did not append a history entry); details taken from the commit diff and the acceptance-criteria annotations in FAQIR-PLAN.md, not from the original session.",
      "reconstructed": true
    },
    {
      "id": "0.7-05",
      "completed": "2026-07-21",
      "commit": "cf6a17a",
      "summary": "Recipe `file-upload` \u2014 drag-drop zone + file list with ZERO network code, which is what keeps the `no-fetch` audit rule honest: the 349-line controller emits events carrying the FileList and uploading is app code. The keyboard/SR path is the real `<input type=\"file\">` (change equivalent to drop, label/click activation), drag states live in data-state, and accept/size validation produces per-file rejection events with reasons; the 223-line manifest documents every event for agent consumption. tests/recipes/file-upload.test.ts (267 lines) simulates drop/dragover/dragleave and the rejection matrix; file-upload-audit.test.ts explicitly asserts the recipe passes `no-fetch`. Mirrored into vue and react (354-line controller each + recipe wrappers); registry-index, skill references, README and ci.yml updated. The 26-controller core reached 39.88 KB gzip, so the size ceiling moved 39 \u2192 41 KB. Follow-up commit 1337bf6 (2026-07-24) regenerated the stale react/vue index snapshots for these bindings. 23 files, +2326/\u221221. Reconstructed from git after the fact (the session that shipped it did not append a history entry); details taken from the commit diff and the acceptance-criteria annotations in FAQIR-PLAN.md, not from the original session.",
      "reconstructed": true
    },
    {
      "id": "0.8-01",
      "completed": "2026-07-26",
      "commit": "c250967",
      "summary": "Breakpoint canon + responsive doctrine. ONE ladder (sm 40rem/640 \u00b7 md 48rem/768 \u00b7 lg 64rem/1024 \u00b7 xl 80rem/1280), min-width-only, replacing three incompatible ones (grid's 640/641-1024 viewport ranges with a real 640.5px dead zone, table's 480/768/1024 container thresholds where sm meant a different number, surface's 640-1400 max-widths). Canon lives in src/utils/breakpoints.ts because media queries cannot read custom properties, so the numbers must be literal in CSS and can only be kept honest by one module that generators, tests and (0.8-10) the audit all read; it is node:*-free so the browser audit bundle can import it. Frozen exports + helpers that structurally cannot emit max-width, and a data-<attr>-<tier> grammar that refuses the five protocol attributes in BOTH directions (responsiveAttribute throws, parseResponsiveAttribute returns null) so data-size-md is unreachable rather than discouraged. FAQIR-SPEC.md gains \u00a715 (ladder table, five rules, grammar, doctrine) and FAQIR-NEXT.md \u00a719 (why layout is agent-critical: ten layout attributes no manifest declares; the intrinsic -> container -> viewport hierarchy; what v0.8 ships) + Decision Summary item 13. The spec is executable documentation: a drift test re-parses the canon table and compares rem and px to the module, \u00a715's CSS fences must use canon min-width only, and its HTML fences pass auditHtmlSource against every registry manifest. The 0.7-08/0.7-09 pattern guards now source 640/1024 from BREAKPOINTS instead of inline literals, keeping the legacy max-width form until 0.8-09 flips it. No registry file touched."
    },
    {
      "id": "0.9-02",
      "completed": "2026-07-29",
      "commit": "3718efa",
      "summary": "The decision, and the word that turned out to be load-bearing. Candidates 1 and 2 (a `flow` primitive, a `data-flow` prop) are wrappers, and the header's own test \u2014 an agent produces spaced output WITHOUT wrapping anything \u2014 eliminates a wrapper before it is tried, so candidate 3 wins: `registry/base/rhythm.css`, one rule at (0,0,0), spending `--section-gap-sm` between consecutive block-level participants inside a flow root. FAQIR-SPEC \u00a720 states it in one sentence and argues each of its three load-bearing words: \"flow root\" (sectioning elements plus `container`/`surface`, never a component that lays out its own children, or every `stack` gap doubles), \"block-level\" (a vertical margin on an `inline-flex` box shifts it inside its line box, so 26 inline-level components are excluded by a list DERIVED from each component's root `display` with a UA-default fallback that catches `link`), and \"consecutive\" \u2014 which was measured, not reasoned: the first draft spaced every non-first child and the printed invoice scaffold came back with every heading floating 48px above its own section, so the owl form now lets a non-participant suppress the margin and an `h2` stays welded to its table. `data-gap` on the flow root re-tunes it and `data-gap=\"0\"` opts out, reusing the primitives' ladder so no new attribute enters the protocol before the 1.0 freeze. The rule is data in `src/utils/layout.ts` and six surfaces print it from there; `tests/utils/rhythm.test.ts` (27 cases) re-parses \u00a720 AND the CSS back into the module and audits \u00a720's examples, `tests/visual/rhythm.pw.ts` (10 cases) proves it in a browser on nested scopes, the surface boundary and the opt-out plus the two guards. Result: seams 183 \u2192 19, seamPages 42 \u2192 5, overlaps 3 \u2192 1, zeroGutterPages held at 79 and asserted, with no registry fragment edited."
    },
    {
      "id": "0.9-03",
      "completed": "2026-08-04",
      "commit": "5071cc1",
      "summary": "Example-page shell: every fragment mounted in a container gutter+measure column and split into captioned <figure> demo blocks \u2014 zeroGutterPages 79 -> 0, seams 19 -> 0; 0.8-13 resolved and the generator now rejects any inline style= it authors."
    },
    {
      "id": "0.9-04",
      "completed": "2026-08-04",
      "commit": "e60fb96",
      "summary": "Reference-fragment sweep: 367 audit findings over 87 fragments driven to 0 and 0.7-17 resolved \u2014 part attribution follows composition, five rules that flagged correct markup fixed, runtime-presence rules scoped to documents by content; layout primitives demo in surface cells; layout-lint floor pinned at 0 seams / 0 zero-gutter."
    },
    {
      "id": "0.9-05",
      "completed": "2026-08-04",
      "commit": "64932bd",
      "summary": "The trigger contract, and the reason nobody saw it was missing. `trigger-contract` (src/audit/rules.ts) says a [data-part=\"trigger\"] is styled by exactly one of two things \u2014 a data-ui it delegates to, or a rule in its own recipe's sheet \u2014 and neither is an error: 16 findings, all four triggers of dialog/drawer/sheet/tooltip, now data-ui=\"button\" like alert-dialog always was. It is the first rule to read markup AND stylesheet, so HtmlAuditInput grew an optional `styles` map (runAudit, the registry gate, createAuditor(manifests, styles)); a component whose sheet the caller cannot produce is skipped, never guessed at, and both halves are asserted so silence is never mistaken for a rule that never ran. Fourth browser scope: markup+css. Then the reason the defect survived: eight recipes documented themselves with a lone trigger. Derived, not listed \u2014 and three of the task's names were wrong: tree-view and file-upload render fine, while dropdown, popover, tooltip and an entirely blank command-palette did not. OVERLAY_PREVIEW_SURFACES forces 10 panels open from styles/faqir.css alone, scoped per page, in flow rather than fixed (four fixed panels would be four overlap findings against 0.9-01's budget, which did not move by one count). Docs-only proved by building the site twice: 86 snippet payloads byte-identical, example pages differing by one attribute, the sheet the same bytes plus one appended section. Rendering the panels found three real defects nothing could have seen: command-palette's group labels at 4.24:1/3.47:1, alert-dialog's destructive title at 4.22:1 in dark, and dialog/drawer/sheet's icon-close rule flattening the footer's delegated Cancel button \u2014 the last fixed with :not([data-ui]), the trigger contract read backwards."
    },
    {
      "id": "1.0R-01",
      "completed": "2026-08-18",
      "commit": "0da2dc3",
      "summary": "Retired the stale `faqir-creator.skill` archive (option a). The zip at the repo root was the skill as of 9 July \u2014 pre-0.5-07, before the manifest-derived generator \u2014 committed but not gitignored, not produced by `gen:skill`, and invisible to `check:skill`: references drifted by ~40 KB against the generated directory, with no layout system, breakpoint canon, plugin list or density axis. Deleted it, gitignored `*.skill` so a packed archive cannot return, and rewrote FAQIR-PROTO-INTEGRATION.md's S1.4 install step to copy `faqir/.claude/skills/faqir-creator/` \u2014 noting it is generated by `gen:skill` and gated by `check:skill`, so an upgrade re-copies rather than hand-edits. Three guards in tests/generator/skill.test.ts: no `.skill` tracked (git ls-files) or on disk at the root; the doc mentions neither `unzip` nor `.skill` and names the directory; and every faqir-repo path the doc cites resolves on disk \u2014 extracted from the doc (backticked, rooted at `faqir/`, `registry/` or `.claude/`, proto-side paths excluded) rather than hardcoded, so a future path added to the doc is checked too."
    },
    {
      "id": "1.0R-02",
      "completed": "2026-08-18",
      "commit": "e90534d",
      "summary": "`references/tokens.md` is generated from `registry/tokens/*.css` instead of hand-maintained. The drift `check:skill` could not see: `--shadow-xs` documented at `/ 0.05` against a source that has read `/ 0.04` for months (and every shadow from `sm` up now two-layer), no surface elevation ramp, no `--measure-*` ladder, no `--leading-loose`, and `--control-height-{sm,md,lg}` mentioned only under Density. Nothing in the new file is transcribed: cascade order comes from `tokens/index.css`'s `@import` list, each section is the `@ui:tokens <group>` header its file carries with the header's own blurb, sub-headings are the source's `\u2500\u2500 Banner \u2500\u2500` runs, px equivalents are the trailing comments, and the `[data-density]` remap is read from the two scope blocks \u2014 a token added to a scope appears without touching the generator. The modifier table is `TOKEN_MODIFIERS` from src/protocol.ts, the constant SPEC-1.0.md \u00a74 is parsed back out against. One trap worth recording: block bodies must be sliced using offsets found on a comment-MASKED copy of the CSS, because `document.css` documents `@page { \u2026 }` inside a comment and that `}` closes the `:root` block after ten of its forty-two tokens. `generateShippedSkillFiles()` now returns five files, so `check:skill` gates this one too. The cross-check runs in the 0.9-12 shape with an independent parser \u2014 every declared token documented, every documented token declared \u2014 and both directions were seeded and observed to fail before passing."
    },
    {
      "id": "1.0R-03",
      "completed": "2026-08-18",
      "commit": "c980b2e",
      "reconstructed": true,
      "summary": "`references/directives.md` became engine + plugin-derived. Back-filled from the commit and the plan's acceptance annotations: the engine declares its own surface in \u00a73.0 of `src/core-src/engine.js` (17 `@ui:directive`, 18 `@ui:modifier`, 12 `@ui:magic` comment lines, zero shipped bytes) and the generator reads them, with a two-way tripwire \u2014 every `l-\u2026`/`$\u2026` the source mentions must be declared and nothing declared that it never mentions. Newly documented: `l-transition`, `l-teleport`, `l-key`, `data-motion`, `.capture`/`.passive`/`.window`/`.document`, nine key aliases, `l-model.debounce`; removed `.ctrl`/`.shift`/`.alt`/`.meta`, which `handleOn` never read. Plugin rows come from `loadPluginMetadata()`. One behaviour finding: `@click.debounce.500ms` is two modifiers and the time is never read \u2014 `.debounce500ms` is the working form. `check:skill` went to 6 files. NOTE: this entry was written by the 1.0R-04 session; the 1.0R-03 session updated FAQIR-PLAN.md but not this cursor."
    },
    {
      "id": "1.0R-04",
      "completed": "2026-08-18",
      "commit": "39ed275",
      "summary": "`references/manifest.md` is now walked out of `manifest.schema.json`, and `faqir create` emits a `$schema`. The old file was the misleading one: nine categories against the schema's eleven (`custom`, `marketing` missing), no `$schema`/`props`/`changes`/`aliases`/`variants.<group>.responsive`, a Button example transcribed at `1.0.0` against a shipped `1.1.0`, and \u2014 the real defect \u2014 no statement that `definitions.componentManifest.required` names ALL SEVENTEEN fields, so its \"Full Schema\" block read as if everything were optional. The generator is generic over the schema document: required/optional split per manifest definition, a row per property with the schema's own type/description (a `$ref` reads the referenced definition's description and constraints through, so `category` states its eleven values on the row that uses it), the nine nested types with their own required columns, every `enum` found by a recursive walk (path label skips `properties`/`definitions` so it reads `variant.responsive`), and the schema's `changelog`. Counted rather than described: the per-kind file set (42 primitives / 29 recipes / 15 patterns; `files.js` on all 29 recipes and nothing else) and the worked example, `button.manifest.json` read off disk with its `@ui:` header comments lifted from `button.html`/`button.css`. The sufficiency criterion is mechanical: a test parses the required table BACK OUT OF THE MARKDOWN, builds a manifest from the field names and types it finds (recursing into the nested tables, `templates` handled by its documented `at least 1 entry` rule) and validates it against both the published schema and `validateManifest` \u2014 seeded by deleting a row and observed to fail. Second half: `create` writes `$schema` as the first property using `add-schema-refs.mjs`'s rule (relative from the manifest's own directory to the project root's `manifest.schema.json`), verified at two `output_dir` depths \u2014 `../../../` at `./ui`, `../../../../../` at `./src/vendor/faqir` \u2014 and it grew `--kind pattern`, since the plan's tests ask for three kinds and the command took two. `generateShippedSkillFiles()` returns 7, so `check:skill` now gates the whole skill directory, and a test compares that set against `readdirSync` of `references/` so an ungated file cannot reappear. Pre-existing and untouched: `docs-agents.test.ts`'s CDN pin vs `packages/core/sri.json` fails identically on a clean tree."
    }
  ],
  "note": "Pointer file for the /faqir-plan slash command. FAQIR-PLAN.md status cells (\u2b1c/\u2705) are the source of truth; this file records the cross-session cursor and a per-task log. Do not hand-edit `current` while a session is running. History is not a complete ledger of every task (phases v0.3\u2013v0.5 are collapsed into one entry); entries flagged `reconstructed: true` were back-filled from git and FAQIR-PLAN.md on 2026-07-25, not written by the session that shipped them."
}
