{
  "$schema": "../../../manifest.schema.json",
  "name": "pricing",
  "version": "1.2.0",
  "kind": "pattern",
  "category": "marketing",
  "description": "Pricing table composing a row-aligned grid of card tiers with stat prices, highlight badges, separators, icon-ticked feature lists, and one call-to-action per tier. Composition only: zero JavaScript. A highlighted tier is data-state='featured' on the tier card.",

  "anatomy": {
    "tag": "section",
    "selector": "[data-ui='pricing']",
    "content_model": "slots"
  },

  "slots": {
    "heading": {
      "selector": "[data-part='heading']",
      "required": true,
      "tag_hint": "h2",
      "description": "Section heading. Give it an id and point the section's aria-labelledby at it"
    },
    "description": {
      "selector": "[data-part='description']",
      "required": false,
      "tag_hint": "p",
      "description": "Optional intro paragraph. Must be a direct child of the section — the tiers' own card descriptions are card slots, not this one"
    },
    "tiers": {
      "selector": "[data-part='tiers']",
      "required": true,
      "tag_hint": "div",
      "description": "The tier grid — a nested data-ui='grid' with data-align-rows and a mirrored mobile-first column ladder. Manifest-aware slot ownership leaves this part and its tier children with pricing because grid declares neither slot"
    },
    "tier": {
      "selector": "[data-part='tier']",
      "required": true,
      "tag_hint": "article",
      "description": "One plan — a direct card child of the tier grid with exactly four direct rows in order: header, divider, body, footer. Inside a tier, those rows and title/description are card slots, never pricing slots"
    },
    "footnote": {
      "selector": "[data-part='footnote']",
      "required": false,
      "tag_hint": "p",
      "description": "Fine print under the tiers (taxes, cancellation, currency)"
    }
  },

  "variants": {
    "columns": {
      "values": ["2", "3"],
      "default": "3",
      "attr": "data-cols",
      "applied_to": "root"
    }
  },

  "states": {
    "default": { "attr": "", "default": true },
    "featured": {
      "attr": "data-state=\"featured\"",
      "applied_to": "tier",
      "description": "Highlights one tier with a primary ring. Pair it with an in-content badge ('Most popular') so the emphasis is not colour-only"
    }
  },

  "a11y": {
    "required_attrs": [
      "aria-labelledby on the section pointing at the heading id",
      "aria-labelledby on each tier pointing at that tier's title id",
      "aria-hidden='true' on the decorative feature tick icons",
      "the featured tier states its emphasis in text, not only with colour"
    ],
    "keyboard": {
      "Tab": "move through the tier call-to-action links in source order"
    }
  },

  "tokens_used": [
    "color-bg", "color-fg", "color-fg-muted", "color-border",
    "color-primary", "color-primary-subtle", "color-primary-fg",
    "space-1", "space-2", "space-3", "space-4", "space-6", "space-8", "space-10", "space-12", "space-16",
    "font-sans", "text-sm", "text-base", "text-lg", "text-3xl",
    "weight-semibold", "weight-bold", "leading-tight", "leading-normal", "leading-relaxed",
    "radius-lg", "shadow-md"
  ],

  "templates": {
    "html": "<section data-ui=\"pricing\" data-cols=\"{columns}\" aria-labelledby=\"{id}-heading\">\n  <h2 data-part=\"heading\" id=\"{id}-heading\">{heading}</h2>\n  <p data-part=\"description\">{description}</p>\n  <div data-ui=\"grid\" data-part=\"tiers\" data-cols=\"1\" data-cols-sm=\"2\" data-cols-lg=\"{columns}\" data-gap=\"6\" data-align-rows>\n    <article data-ui=\"card\" data-part=\"tier\" aria-labelledby=\"{id}-tier\">\n      <div data-part=\"header\">\n        <h3 data-part=\"title\" id=\"{id}-tier\">{tier_name}</h3>\n        <p data-part=\"description\">{tier_blurb}</p>\n        <div data-ui=\"stat\" data-size=\"lg\">\n          <div data-part=\"value\">{price}</div>\n          <div data-part=\"label\">{period}</div>\n        </div>\n      </div>\n      <hr data-ui=\"separator\" data-part=\"divider\">\n      <div data-part=\"body\">\n        <ul>\n          <li><span data-ui=\"icon\" data-icon=\"check\" aria-hidden=\"true\"></span>{feature}</li>\n        </ul>\n      </div>\n      <div data-part=\"footer\">\n        <a data-ui=\"button\" data-variant=\"outline\" href=\"{href}\">{cta}</a>\n      </div>\n    </article>\n  </div>\n  <p data-part=\"footnote\">{footnote}</p>\n</section>"
  },

  "safe_transforms": [
    "add-tier",
    "remove-tier",
    "change-column-count",
    "move-featured-state-to-another-tier",
    "change-prices-and-periods",
    "add-feature-to-a-tier",
    "change-tier-call-to-action",
    "add-footnote"
  ],

  "unsafe_transforms": [
    "add-pricing-data-parts-inside-a-tier-card",
    "remove-aria-labelledby",
    "signal-the-featured-tier-with-colour-only",
    "replace-feature-lists-with-divs",
    "add-custom-script"
  ],

  "composition": {
    "contains": ["grid", "card", "badge", "stat", "separator", "button", "icon"],
    "used_in": ["landing-page"],
    "notes": [
      "A data-part belongs to the nearest enclosing component that declares it. The nested grid declares neither tiers nor tier, so those remain pricing slots; inside a tier, header/divider/body/footer belong to card.",
      "The price is a nested data-ui='stat' (value = amount, label = period). Pricing re-orders the stat label so the amount leads and uses auto space above the stat so prices share the bottom edge of the aligned header track.",
      "data-align-rows fixes four direct card rows (header, divider, body, footer) per wrapped grid line. Without subgrid the grid mode makes every tier span the full width, so the fallback is a correct intrinsic stack rather than misaligned side-by-side cards.",
      "`faqir scaffold landing-page` copies the block between the <!-- @ui:scaffold landing-page --> markers in pricing.html verbatim."
    ]
  },

  "files": {
    "html": "pricing.html",
    "css": "pricing.css",
    "manifest": "pricing.manifest.json"
  },

  "tests": [
    "renders-as-section-with-data-ui-pricing",
    "heading-and-tiers-slots-are-required",
    "every-tier-is-a-card",
    "the-price-is-a-stat-primitive",
    "featured-tier-carries-data-state-featured",
    "featured-emphasis-is-also-stated-in-content",
    "feature-ticks-are-aria-hidden-icons",
    "ships-zero-javascript",
    "one-tier-per-row-below-sm-two-from-sm-and-the-declared-count-from-lg",
    "tier-grid-declares-four-row-card-alignment",
    "non-subgrid-fallback-stacks-complete-tier-cards",
    "reference-page-is-audit-clean"
  ],

  "changes": [
    {
      "version": "1.2.0",
      "note": "Tier containers now compose the grid primitive with its declared data-align-rows mode. Every tier card exposes four direct rows (header, divider, body, footer); supporting engines share those tracks through subgrid, and the price is bottom-aligned inside the shared header track. The grid's no-subgrid fallback stacks complete cards at full width, preserving truthful content relationships. The root data-cols contract remains mirrored into the nested grid ladder.",
      "breaking": false
    },
    {
      "version": "1.1.0",
      "note": "The responsive ladder moves onto the breakpoint canon, mobile-first (task 0.8-09). The sheet's max-width media blocks became min-width floors on canon tiers (sm 40rem/640px, md 48rem/768px, lg 64rem/1024px), so every unconditional rule is now the PHONE case and a tier block only ever widens it. The tier grid follows grid's mobile-first semantics (0.8-04): data-cols names the WIDEST count, reached at lg, with two columns from sm and one on a phone. The floors keep their values, so the only behavioural shift is at exactly a floor: 1024px now gets the wide layout rather than the narrow one.",
      "breaking": false
    }
  ]
}
