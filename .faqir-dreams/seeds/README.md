The seed a dream is written from lands here as <brief-id>.seed.json.
It is committed with the theme it produced, so a kept dream is reproducible:
`faqir theme generate --seed .faqir-dreams/seeds/<id>.seed.json --out registry/themes`
